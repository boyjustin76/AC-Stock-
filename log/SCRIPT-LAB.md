# 대본 실험실 (E 세션)

> 이 파일은 대본 담당 E 세션의 보고서다. 초안·완성 대본은 `scripts/shortform/` 에 있다.
> 여기 적힌 숫자는 전부 드라이브 실측이다.

---

## 2026-08-28 — 포인트_차 역설계 · [포인트_차]진화한 세력들의 악랄한 움직임 초안

### 0. 오늘 받은 일

| 건 | 상태 |
|---|---|
| SL #11-4 20일선 추세추종 매매법 | 기존 (중간) 초안 유지 — 팀장 컨펌 대기 |
| SL #11-5 20일선 박스권 매매법 | 기존 (중간) 초안 유지 — 팀장 컨펌 대기 |
| 포인트 1편 — [포인트_차]진화한 세력들의 악랄한 움직임 | **신규 작성 완료 (중간)** |

포인트 편은 매뉴얼(§1 자산 지도)에 없는 새 갈래다. `shortform_rule`·`shortform_part`·
`naming_rule` 은 전부 **SL 전용**이라 그대로 쓸 수 없었다. 그래서 드라이브의
`차트명가(숏)` 43개 포인트_차 폴더를 전수 실측해서 기준선을 새로 냈다 (§1).

---

### 1. 기준선 — **New 창(260725~261001)** (팀장 지시)

기준선을 세 번 옮겼다. 최종은 팀장 지시대로 **`260725_[포인트_차]포지션 중독` 부터**다.
여기서 스타일 개편이 시작됐고, **그 이전은 Old — 참고만 하고 기준으로 삼지 않는다.**

New 창 11편 중 `260801 볼린저밴드 매매법` 은 `260430` 과 대본이 공백 제외 md5 동일한
**재업**이라 뺐다. **신작 10편**이 모집단이다.

| 항목 | **New 10편** | (참고) Old 포함 43편 |
|---|---|---|
| 영상 길이 중앙값 | **53.9초** (43.1~63.1, n=7) | 54.9초 |
| 발화 글자수 중앙값 | **362자** (267~495) | 384자 |
| 발화 속도 중앙값 | **6.70자/초** | 7.41자/초 |

**갈래별로 속도가 다르다 — 이게 중요하다.**

| 갈래 | New 편 | 발화/길이 | 속도 |
|---|---|---|---|
| **토크(라이브톤)** | 260725 포지션 중독 · 260730 5분봉 30이평선 | 415자/60.0초 · 495자/53.9초 | **6.91 · 9.19** |
| 기법 | 260806 · 260808 · 260810 · 260917 · 261001 | 319~482자 | 6.49~7.63 |
| 모드 A 복제 | 260903 · 260910 · 260924 | 267~352자 | 5.71~6.19 |

토크편은 말이 빨라서 같은 초에 글자가 더 들어간다. **토크편 분량은 415~495자로 잡는다.**

이름 규칙(New 11편 전수 일치, DB `naming_rule` 에 없음):
폴더 `YYMMDD_[포인트_차]제목` · 파일 `[포인트_차]제목.txt`.

### 2. [포인트_차]가 [포인트]를 카피·변형하는 알고리즘

트팩↔차명 짝 7건을 붙여서 10자 n-gram 겹침을 쟀다. **모드가 두 개로 갈린다.**

| 짝 | 트팩 원본 | 차명 | 간격 | 압축률 | 10자 겹침 |
|---|---|---|---|---|---|
| RSI+MACD | 260227 | 260924 | 7.0개월 | 1.01 | **70.6%** |
| FVG 단타 | 260218 | 260903 | 6.5개월 | 0.94 | **68.9%** |
| 캔들 꼬리 | 260322 | 260910 | 5.6개월 | 0.87 | **59.7%** |
| 구름대 공략 | 251101 | 260808 | 9.2개월 | 1.13 | **47.8%** |
| 골든크로스 | 260728 | 261001 | 2.1개월 | 0.92 | 5.4% |
| 후행스팬 | 260814 | 260810 | ~동시 | 0.87 | 3.0% |
| 왜 상승은 같은 모양 | 260513 | 260528 | 0.5개월 | 0.59 | 1.4% |

**모드를 가르는 건 원본과의 시간 간격이다.**

- **모드 A — 근접 복제 (간격 5.5개월 이상, 겹침 48~71%)**
  본론 문장을 거의 그대로 가져온다. 이 4편은 전부 차명 폴더 안에
  `TF(참고)_<트팩폴더명>` 폴더가 통째로 들어 있다. 카피인 걸 숨기지도 않는다.
- **모드 B — 뼈대만 복제 (간격 2개월 이하, 겹침 1~5%)**
  주제·논리 순서·근거는 같은데 문장은 전부 다시 쓴다. 참고 폴더도 없다.
  겹침 1.4~5.4% 는 저장소가 SL 에 쓰는 기준선(2.2%)과 같은 수준이다.

즉 **"두 채널에 비슷한 영상이 가까운 시점에 같이 나가지 않게" 하는 게 규칙**이다.
묵힌 주제는 복붙하고, 최근 주제는 다시 쓴다.

방향도 폴더가 증언한다 — 참고 폴더가 어느 쪽에 들어 있느냐로 갈린다:
- 차명이 트팩을 베낌 (`TF(참고)_` 가 차명 폴더에): 구름대·FVG·캔들꼬리·RSI+MACD — 전부 묵은 기법 편
- 트팩이 차명을 베낌 (`참고_..._[포인트_차]` 가 트팩 폴더에): 긁히는 것(260708→260811)·후행스팬(260810→260814)·테스타 이평선(260806→260904)
  → 이 셋은 트팩 폴더에 대본 대신 **라이브 질문 브리프**만 있다. 라이브에서 나온 주제는 차명이 먼저 만들고 트팩이 받는다.

### 문장 단위 변형 규칙 — 후행스팬 짝(요청 건) 기준

| 자리 | [포인트] 260814 | [포인트_차] 260810 | 규칙 |
|---|---|---|---|
| 제목 | 세력도 못 속이는 일목균형표 후행스팬 실전 매매법 | (동일) | **제목은 안 건드린다** |
| 훅 | "복잡하기만 했던 일목균형표, 이제 선 하나만 남기고 전부 지우세요." | "오늘은 일목균형표로 단타 타점 잡는법을 알려드릴게요 / 정말 쉬우니 꼭 따라해보세요." | 명령·선언 → **"오늘은 ~ 알려드릴게요" + 쉽다는 안심** |
| 근거 | "26봉 전 차트와 직접 대조해 주는 지표입니다" | "단순하게 요약하자면 / 이 선은 현재 종가를 선으로 그어서 / 왼쪽으로 26캔들 뒤에 그려둔 것입니다" | 한 문장을 **호흡 단위로 쪼개고 풀어 씀** |
| 본론 | "그렇다면 대체 왜 굳이 26일 뒤에 선을 그어두었을까요?" | "그럼 대체 26일 뒤에 왜 선을 그어두었을까요?" | 어순만 바꾼 **동의 치환** |
| 용어 | 휩소 · 확정적으로 · 폭발하는 · 매물대 | (휩소 삭제) · 확실하게 · 전환되는 | **전문어·과장어 한 단계 낮춤** |
| 지문 | (자료 화면: 차트 강조 연출) | (삭제) | 트팩 연출 지문은 안 가져온다 |
| 결론 | "여러분도 차트에 적용해서 꼭 한번 실전에 사용해 보세요." | "여러분도 꼭 한번 사용해보세요. / **그리고 구독을 누르고 더 많은 매매법을 받아보세요**" | **CTA 를 반드시 갈아끼운다** |
| 분량 | 716자 | 605자 (0.85) | 짧아진다 |

다른 짝에서 반복 확인된 것:
1. **CTA 치환이 예외 없다.** 트팩은 `영트모로 입장하세요` / `아래 영상을 클릭하고`(커뮤니티 유도),
   차명은 `저를 구독하고 다음 영상을 기다려주세요` / `저를 팔로우하시고`(구독 유도).
   7편 중 7편이 그렇다. **이것만은 절대 안 가져온다.**
2. **결론에서 답을 뺀다.** 골든크로스는 트팩이 "장기선으로 추세 읽고 나머지 2선으로 크로스"까지
   구체적으로 알려주는데, 차명은 그 대목을 지우고 "어떠한 세팅값으로 사용해야할까요? / 자세한 내용은
   댓글과 아래 영상을 참고해주세요" 로 끝낸다. 왜상승은 아예 연장파동 설명 도중에 끊고
   "지금 비트코인 차트는 몇번째 파동일까요?" 로 넘긴다 → **정보 유보 + 참여 질문**.
3. **제목은 유지하거나 자극을 한 단계 올린다.** `계좌를 녹이는 골든 크로스` → `계좌를 녹이는 골든크로스의 함정`.
4. **트팩이 들고 온 권위·수식은 지운다.** `코인 대회 우승자들이 수익을 쓸어 담을 때 사용하는 FVG` → `FVG 매매법`.
5. **차명은 지문을 대본에 남긴다.** `(차트보며)` · `(*강한 추세 구간 이미지 사용)` ·
   `(**이번 편은 말하듯이 편안한 대화톤으로 녹음 부탁드립니다)` — 녹음 톤 지시까지 대본에 적는다.
6. **차명 화법 고정구** (`차트명가_숏츠 프롬프터 학습용 데이터.txt` 19,742자와 교차 확인):
   `오늘은 ~ 알려드릴게요` / `~인데요` / `만약 ~라면?` (질문으로 끊고 답) /
   `정리하자면 ~ 라는걸 기억해주세요` / `~하시면 됩니다`.

---

### 3. New 표준 — 신작 10편

| | 260725 | 260730 | 260806 | 260808 | 260810 | 260903 | 260910 | 260917 | 260924 | 261001 |
|---|---|---|---|---|---|---|---|---|---|---|
| 첫 줄 `제목 : ` | ○ | ○ | · | ○ | ○ | · | · | · | · | ○ |
| 라벨 `① 훅 (Hook) : ` | ○ | ○ | ○ | ○ | ○ | · | · | ○ | · | ○ |
| 톤 지시 줄 | ○ | ○ | · | · | · | · | · | · | · | · |
| 훅 `오늘은~알려드릴게요` | · | · | · | · | ○ | ○ | · | · | · | ○ |

- **첫 줄 `제목 : <제목>` 5/10.** 나머지 5편은 전부 **원본 URL 을 들고 오는 편**이다
  (`레퍼런스`+URL · `[대괄호 제목]`+URL). **URL 을 안 쓰면 `제목 : ` 형이고, 섞은 편은 0.**
- **라벨 `① 훅 (Hook) : ` — 콜론 뒤 공백 하나. 7/10.** 콜론 없는 3편은 URL 형과 겹친다.
- **④ 는 `결과 (Conclusion)` / `결론 (Conclusion)` 둘 다 쓴다.**
- 훅 `오늘은 ~ 알려드릴게요` 는 3/10 뿐이다. 선언형·질문형·대화형이 섞인다.

#### 3-1. 토크(라이브톤)편 — 이번 편이 속하는 갈래

260725·260730 두 편만 제목 밑에 `**라이브 방송 중 답변한다는 느낌으로` 가 붙는다.
**팀장 판단: 원본이 라이브에서 딴 것이면 라이브 느낌을 죽이지 않는다.** 앞서 나는
"차명은 라이브가 없다"는 사실을 **문구를 빼라는 뜻으로 잘못 읽고** 3·4차에서 이 톤을
전부 걷어냈다. 그게 아니라 **연기 지시**다 — 라이브에서 답하듯 말하라는 것이지
차명이 라이브를 한다는 주장이 아니다. 되살렸다.

| 항목 | 토크편 (260725·260730) |
|---|---|
| 2행 | `**라이브 방송 중 답변한다는 느낌으로` |
| 훅 | 대화로 던진다 — `솔직히 이 영상은…` · `이게 왜 먹히는지는 모르겠는데..` |
| 종결 | 구어 — `~거든요` `~잖아요` `~단 말이죠` `~있어요` `그러니깐`, 중간에 반말 단정 |
| 질문 화법 | 던지고 바로 답한다 — `우리가 트레이딩을 하는 진짜 목표는 뭔가요?` |
| 비유 | 260725 야구 배트 |
| 지문 | `(차트보며)` · 반복 지시 `여기서도 한번 / 여기서도 한번`(260730) |
| 결론 | `정리하자면` → 반말 단정 슬로건 → `동의하면 구독과 좋아요` / `반박하면 댓글도 환영입니다` |

---

### 4. 이번 초안 — [포인트_차]진화한 세력들의 악랄한 움직임

- 원본: `260809_[포인트]진화한 세력들의 악랄한 움직임` (트팩, 88.2초 / 발화 660자)
- 결과물: `scripts/shortform/(중간)260828_[포인트_차]진화한 세력들의 악랄한 움직임/`

원본은 `① 도입부 / ② 본문 / ③ 결론` 3단에 구어 전사체(`짰잖아요?` `말이에요`
`던져버린다고요`)이고 파일 머리에 롱폼 `watch?v=` 링크가 붙어 있다 — **발화물에서 딴 것**이다.
폴더에 라이브 질문 브리프는 없지만 문체가 그렇다. 그래서 §3-1 토크(라이브톤)편으로 썼다.

#### 형식 대조

| 항목 | 260725 | 260730 | 이번 초안 |
|---|---|---|---|
| 첫 줄 `제목 : ` | ○ | ○ | ○ |
| 2행 `**라이브 방송 중 답변한다는 느낌으로` | ○ | ○ | ○ |
| 라벨 `① 훅 (Hook) : ` | ○ | ○ | ○ |
| `④ 결과 (Conclusion) : ` | ○ | ○ | ○ |
| `(차트보며)` | ○ | · | ○ |
| 비유 | ○ (야구) | · | ○ (낚시) |
| `정리하자면` → 슬로건 → couplet | ○ | ○ | ○ |
| URL 줄 | 없음 | 없음 | 없음 |

#### 숫자

| 지표 | 이번 초안 | New 토크편 | 판정 |
|---|---|---|---|
| 발화 글자수 | 419자 | 260725 415자 · 260730 495자 | 범위 안 |
| 추정 길이 | **60.6초** (@6.91=260725 속도) ~ **52.0초** (@8.05=토크 평균) | 60.0초 · 53.9초 | 범위 안 |
| 원본 대비 압축 | 0.63 | 모드 B 0.59~0.92 | 범위 안 |
| 10자 겹침 | **0.0%** | 모드 B 1.4~5.4% | 기준선보다 낮음 |
| 6자 겹침 | 3.9% | 모드 B 3.7~12.4% | 범위 안 |

#### 팀장 피드백 반영

1. **표현을 포인트_차 색깔로.** 질문 던지고 답하기(`그런데 지금은 어떻습니까?`) ·
   비유(낚시로 물고기 지치게 만들기 — 260725 야구 배트에 대응) · 반복 지시 화법
   (`여기서도 한번, 여기서도 한번` — 260730) · 구어 종결(`~거든요` `~잖아요` `~단 말이죠`
   `그러니깐` `~죠?`)을 넣었다.
2. **라이브 느낌 유지.** `**라이브 방송 중 답변한다는 느낌으로` 를 되살리고, 3·4차에서
   `~습니다` 정돈체로 눌러 놓은 문체를 토크편 구어로 되돌렸다. 원본의 `말려 죽이는` 도
   슬로건에 살렸다 — 제목의 '악랄한' 과 맞는 유일한 표현이라 판단했다. 빼도 무방하다.
3. **New(260725~) 기준.** Old(260429~260718)는 참고만 했다. 4차에서 모델로 삼았던
   260708·260715 는 Old 라 기준에서 뺐다.

**다음 편 주제(손절 기준)는 슬로건 3행에 넣었다** — `그러니깐 버티지 말고 손절 기준부터 잡자`.
토크편 결론에는 질문 자리가 없어서(260725·260730 둘 다) 이 형태가 맞다.

#### 초안 이력

| 차수 | 기준선 | 결과 |
|---|---|---|
| 1차 | 트팩→차명 변형 규칙만 | 형식 기준을 트팩에서 찾은 것이 오류. `TF(참고)` 줄·URL·지어낸 톤 지시문 |
| 2차 | 260725·260730 | 방향은 맞았으나 본문에 `제가 라이브에서도 자주 말씀드리는건데` 를 넣어 없는 사실을 말함 |
| 3차 | 260514 | 라이브 문구를 전부 제거 — **과잉 교정.** 게다가 260514 는 Old(4월) |
| 4차 | 260708~261001 | 창은 좁혔으나 Old 2편(260708·260715)을 모델로 삼아 정돈체가 됨. **컨펌 통과** |
| **5차 (현재)** | **New 260725~261001** | 라이브톤 복원 + 포인트_차 화법 장치 투입 |

---

### 4-1. 팀장 판단이 필요한 것

1. **방영일 — 미확정.** `(중간)` 과 `260828` 그대로 둔다.
2. **`말려 죽이는`.** 원본 표현을 슬로건에 되살렸다. New 10편에는 이 정도로 센 표현이 없다
   (최고 수위가 260725 `나중에 큰 일 납니다`). 빼려면 `달라진건 털어내는 방식뿐이다` 로 바꾸면 된다.
3. **분량.** 419자로 260725(415자·60.0초)와 사실상 같다. 260725 속도면 약 60초다.
   더 줄이려면 `마이너스 80%까지 밀었다가 / 본전 근처까지 올려주고, 또 밀고.` 두 줄(약 30자)을 빼면 된다.
4. **소스.** 트팩 원본 폴더 `소스+원본` 에 나스닥 1분 차트 영상·GIF 가 다 있다.

---

### 5. tools/shortform.py — 포인트 편에는 그대로 못 쓴다 (고치지 않았음)

`check` 를 돌린 결과다. 내용 규칙 5개는 전부 ○.
분량과 이름 판정은 **도구가 SL 기준이라 못 믿는다.**

| 오판 | 원인 | 실측 |
|---|---|---|
| "486자 → 약 71초" | ① `norm()`(38행)이 파일 전문을 센다 — 참고 헤더·URL·녹음 지문·`훅 Hook` 같은 라벨까지 발화로 계산 ② 속도가 SL 값 6.82자/초 고정 | 발화 419자 · 약 60초 |
| "폴더/파일 이름이 회사 규칙에 어긋남" | `naming_rule` 이 SL 전용(`[SL_차XX_#X]`)이다. 포인트 규칙이 DB 에 없다 | 드라이브 43편 전수: `YYMMDD_[포인트_차]제목` / `[포인트_차]제목.txt` |
| "'포인트/포' 표기를 쓰지 않는다" 위반 | 파일명의 `[포인트_차]` 를 자막 문구로 오인 | 포인트 편은 이 표기가 이름 규칙 그 자체다 |

**일부러 어긴 규칙 (매뉴얼 §3 대로 이유를 적는다).**
- 필수 `«오늘은 …를 알려드릴게요» 로 시작` — **New 토크편 2편(260725·260730)이 2편 모두
  이 공식을 안 쓴다.** `**라이브 방송 중 답변한다는 느낌으로` 를 달아 놓고 강의 공식으로
  시작하면 톤 지시와 어긋난다. 4차(정돈체)에서는 통과했지만, 팀장 지시로 토크편으로 되돌리며 다시 어겼다.
- 권장 `아래/다음 영상 유도` · 선택 `CTA 미해결 질문` — 토크편 결론이
  `동의하면 구독과 좋아요 / 반박하면 댓글도 환영입니다` 로 끝나서 자리가 없다. 260725·260730 과 같다.

**나간 편으로 검증했다.** 이미 방영된 `260810_[포인트_차]후행스팬` 을 그대로 넣으면
check 는 "401자 → 59초" 라고 하는데 실제 자막은 **53.1초**, `260910_[포인트_차]캔들 꼬리` 는
"334자 → 49초" 인데 실제 **43.1초**다. 나간 편에도 11~14% 길게 나온다.

**도구는 고치지 않았다.** 매뉴얼 §5 에서 `tools/shortform.py` 는 내 영역이지만, 오늘 받은 일은
대본 3편이고 포인트 갈래를 도구에 새로 넣는 건 범위 밖이라 판단했다. 넣을 거라면 필요한 값은 위에 다 있다:
`--kind point` 로 ①발화만 세기(참고 헤더·URL·`(**...)` 지문·라벨 제외) ②cps 7.19(토크)/6.60(기법)
③`YYMMDD_[포인트_차]제목` 이름 규칙 ④'포인트' 표기 검사 해제. **총괄 승인 후에 넣겠다.**

---

### 6. 이번에 확인한 함정

- 포인트 대본 파일은 **UTF-8 BOM** 이다. BOM 없이 읽으면 첫 줄이 깨진다.
- **사실을 규칙으로 넘겨짚지 마라.** "차명은 라이브가 없다"는 사실을 듣고 `**라이브 방송 중
  답변한다는 느낌으로` 를 빼야 한다고 결론냈는데, 그건 **연기 지시**였다. 사실 하나가 어떤
  규칙으로 이어지는지는 지시한 사람이 정한다 — 내가 추론해서 3·4차를 날렸다.
- **전수(全數)가 항상 옳은 건 아니다.** 43편을 다 세는 게 맞다고 보고 §3 을 냈는데, 첫 줄
  형식이 7월에 갈아탔고 발화 속도도 7.41 → 6.70 으로 달라져서 **옛날 편이 분포를 끌어당겼다.**
  전수는 '무엇이 변했나'를 찾는 데 쓰고, **기준선은 최근 창에서 뽑아야 한다.**
- **모집단에서 재업과 파생을 걸러라.** 창 14편 중 2편은 옛 파일 재업(md5 동일), 2편은 트팩
  라이브 파생이었다. 안 거르면 12편 중 4편, 즉 3분의 1이 오염된 기준선이 된다.
- **형식 기준선을 트팩에서 찾으면 틀린다.** 1차는 §2(트팩→차명 변형)만 보고 썼는데 그건 내용 규칙이다.
- 짝을 하나만 보면 틀린다. 요청받은 후행스팬 짝(겹침 3.0%)만 봤다면 "차명은 항상 다시 쓴다"로
  결론 냈을 텐데, 7짝을 다 재보니 절반이 겹침 48~71% 였다. 매뉴얼 §4-1 그대로다.
- 후행스팬 짝은 **차명이 원본이고 트팩이 받은 쪽**이다(트팩 폴더에 `참고_260810_[포인트_차]` 가 있다).
  요청 문구는 반대 방향을 전제했는데 실물은 그렇지 않았다. 방향은 참고 폴더가 어디 있는지로 판정했다.
