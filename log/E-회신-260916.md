# 문의서 회신 — E (대본·컷편집·더원트레이더) · 2026-09-16 15:00 KST

총괄 문의서(2026-09-16 14:38)에 대한 답. 형식은 요청대로 **번호 · 답 · (확인 시각)**.
재보고 답한 것은 명령까지 적었다. 모르는 것은 모른다고 적었다.

---

## 1. 공용 도구 회귀

**1-1. srt_rules.py 회귀 — 확인했다. 판정 안 바뀜.** (14:45)

본류 판(`c4c82e1`)과 지금 판(`84bba48`)으로 차11-4·차11-5 납품 srt 를 각각 돌렸다.

| 파일 | 본류 판 | 지금 판 |
|---|---|---|
| 차11-4 | 3건 — 15자 #16·#31 · 의존명사 #6 | **같은 3건** |
| 차11-5 | **검사 안 함** (첫 파일에서 멈춤) | 4건 — 16자 #28 · 15자 #29·#34 · 의존명사 #33 |

차11-5 의 4건은 새 규칙이 만든 게 아니라 **`all(...)` 단락평가 버그로 묻혀 있던 것**이다
(그 버그는 `c237994` 에서 고쳤고, 총괄이 읽은 19커밋에 들어 있다).
오늘 새로 넣은 **시각 검사**(끝≤시작 · 겹침 · 0.3초 미만)는 두 편에서 **0건**이다.
나누기 로직 회귀(`cuetune`)는 **35/53 그대로**(13:05).

**1-2. 두 시리즈가 도구를 나눠 쓰지 않는다. `cut_and_srt.py` 하나다.** (14:47)

- `tools/cutedit/build_cuts.py` 는 차11-4·5 때 쓴 **1세대 스크립트**다 (폴더 안 `cam.mp4` 전제,
  컷리스트·내레이션 wav·참고 mp4·srt 를 한 번에 뽑던 것). 그 뒤 차트명가 숏폼 컷편집 작업은
  내 브랜치에 없다 — 즉 **이 파일은 09-01 이후 쓴 적이 없다.**
- 지금 살아 있는 `build_cuts` 는 **`cut_and_srt.py` 안의 함수**(151줄)다. 이름이 같아 헷갈린다.
- 제안: 파일 쪽을 `tools/legacy/` 로 옮기거나 상단에 "레거시 — `cut_and_srt.py` 를 쓴다" 한 줄.
  총괄이 정해 주면 따르겠다. 지금은 손대지 않았다.

**1-3. 채점대와 도구, 오늘 맞췄다 — 일치.** (14:44)

흉내가 맞는지 말로 확인하지 않고, **실제 도구가 뱉은 `cuts.json` 을 정답 XML 에 직접 채점**해
tune 이 말하는 숫자와 맞춰 봤다.

| | 경계 | 평균오차 | 최대 |
|---|---|---|---|
| S015 | 24 | 0.0572초 | 0.173 |
| S016 | 26 | 0.0455초 | 0.278 |
| **합계(도구 출력 직접 채점)** | **50** | **0.0511초** | **0.278** |
| `tune.py --current` | 50 | 0.051초 | 0.28 |

덧붙임 — 오늘 `cut_and_srt.py` 를 고쳤지만(타임라인 자리를 초→프레임) **컷 값은 안 바뀐다.**
s015·s016 `cuts.json` 바이트 동일 확인(13:02). OUT 손잡이 규칙(09-11 추가)도 컷 채점 0.0511 그대로였다.

---

## 2. 상단 배너 모델

**2-1. `--report` 수치.** (14:50)

- **대본이 붙는 회차 8편** (S006·S007·S008·S009·S011·S012·S015·S016) → 값 16개(윗줄 8 + 아랫줄 8)
- **대본 전체와의 유사도**: 중앙값 **0.534** · 최소 0.367(S015 윗줄) · 최대 0.685(S009 윗줄)
  · 윗줄 평균 0.491 · 아랫줄 평균 0.546 (아랫줄이 +0.055 더 가깝다)
- **가장 가까운 '문장'과의 유사도**: 값 16개 · 중앙값 **0.634** · 0.474~0.763
- 표본이 16개라 **십분위는 의미가 없다.** 상·하위 2개로 대신한다 —
  상위 0.685·0.627 / 하위 0.367·0.403

**2-2. 홀드아웃 — 안 했었다. 지금 했다.** (14:52)

한 편을 빼고 나머지 7편으로 띠를 그은 뒤 뺀 편이 그 안에 드는지 (leave-one-out, 8편).

| | 띠 안 | 벗어난 편 |
|---|---|---|
| 윗줄 | 6/8 | S009 0.685 · S015 0.367 |
| 아랫줄 | 6/8 | S012 0.592 · S006 0.496 |

**이 수치를 예측력으로 읽으면 안 된다.** 8점짜리 min~max 띠는 극단값이 반드시 벗어나고,
실제로 벗어난 4개가 전부 각 줄의 최대·최소다. 쓸모 있는 것은 **띠 폭**이다 —
**아랫줄 0.496~0.592 (폭 0.096)** 는 좁아서 제약으로 쓸 만하고,
**윗줄 0.367~0.685 (폭 0.318)** 는 넓어서 못 쓴다. 문서의 "아랫줄에는 상한이 있다"는 이쪽만 지지된다.

**2-3. S016 3안 — 채택된 것 없음. 팀장 확정본은 따로 나왔다.** (14:53)

3안(① 통념 도발 ② 단순화 약속 ③ 세 구간 대비)을 유사도까지 채점해 냈다.
실제 확정본은 **「구름대만 남기세요 / 복잡한 일목균형표」**(선배 확정)이고, 이게
1판 규칙("아랫줄 = 최다 출현어")을 깨서 **2판이 나오는 계기**가 됐다.
내 3안에 대한 팀장·이정찬의 개별 반응은 기록에 없다 — **모름**.

**2-4. 2판 공식은 8편 중 5편에서 맞는다.** (근거: `숏폼_프로젝트모음/상단배너_공식.md` 증거표)

- 두 줄 다 뒤집힘 **5편** — S016·S007·S011·S012·S015
- 예외 2편 — S009(앞뒤 안 바뀜) · S008(첫 문장이 아니라 본문·결론에서 옴)
- S006 은 윗줄만 맞고 아랫줄은 약함(0.547)
- **분모는 13이 아니라 8이다.** 배너는 13편이지만 대본이 붙는 것이 8편이라 공식을 잴 수 있는 게 8편뿐이다.

---

## 3. 롱폼 L08

**3-1. 반려·피드백 있었다. 세 번.**
1. **09-11 구조 정정 (이정찬)** — PD 영상을 캠 컷 위 시각자료로만 얹은 첫 판이 촬영 구조를
   잘못 안 것이었다. 폐기하고 합본(캠 형광 줄 + PD 나레이션 소리 + 포인터 시연 화면)으로 다시 만들었다.
   그때 만든 `pd_overlay.py` 는 지웠다.
2. **09-11 오후 프리미어 불량 3건** — 시연 클립 깜빡임(XML 트랙 순서) · V2 시연 두 토막(같은 트랙 겹침)
   · 자막 1:41 끊김(줄 끝이 컷 밖으로 나가 다음 컷 머리로 튐). 같은 날 고쳐 재납품.
3. **09-16 소스 오프라인** — 아래 3-3.

**3-2. 롱폼 자막 정답본은 못 받았다.** 21자 상한은 **관측값**(L04·L05 최종본 자막 21개)인 채로 둔다.
지금 정답 자료는 숏폼 S015·S016 수정본뿐이고, 저장소 `tools/cutedit/grade/truth/` 에 있다.

**3-3. 09-16 오프라인 사고 — 두 번 났다.** (파일 수정시각 기준)

| | 1차 | 2차 |
|---|---|---|
| 원인 | `차트명가 NEW\` 폴더 이동 | AE 꾸러미 3폴더를 `02_AE작업실_aelab\pack\` 으로 합침 |
| 끊긴 것 | 10개 (PNG 2·AEP 2·MP3 4·PNG 2) | 4개 (PNG 2·AEP 2) |
| 발견 | E 가 13:2x 에 `.prproj` 를 읽어 발견 | **이정찬이 14:08 에 프리미어로 열다가** |
| 복구 | D, 13:27:48 | D, 14:12:58 |
| E 검증 | 13:3x — 본편 32 · 수정본 65 모두 연결 | 14:56 — 모두 연결 · 옛 폴더 이름 무는 프로젝트 0개 |

**이정찬이 열어 본 것은 14:08(끊긴 상태)이 마지막이다. 14:12 복구 뒤로는 아직 안 열어 봤다.**
2차 복구 확인은 내 검사 결과로만 되어 있다.

**3-4. 한 회차 1.1MB.** (14:56)

`tools/theone/longform/L08` = 1.1MB — pd 393K(전사+무음) · `pd_motion5fps.json` 332K ·
cam 226K · pdnar 52K · full 48K · script.txt 12K.
대부분이 **STT 전사 JSON(낱말 시각 포함)** 과 **움직임 파일**이다.
롱폼은 회차당 1~1.5MB, 숏폼은 100KB 안팎으로 본다. 전사 JSON 을 빼면 절반 이하가 되지만
그러면 정렬·자막을 다시 만들 수 없어 남겼다. 원본 영상·wav 는 애초에 안 넣는다.

---

## 4. DB 등재

**4-1. 올려도 된다. 다만 표본을 같이 적어 달라.**
- 회차 이름 `SNNN_제목_YYMMDD` · 자료 위치 `더원트레이더_JG/숏폼_프로젝트모음` — 확정.
- 초당 5.43~5.91자 — **자막이 붙는 편 기준**이다.
- 배너 공식 — **13편 중 대본 붙는 8편** 기준이고, 두 줄 다 맞는 것은 5편이다(2-4).
- 컷·자막 수치는 S015·S016 **두 편** 채점이다. 롱폼 정답본이 오면 다시 잰다.

**4-2. 폴더 규칙 — 관측은 이렇고, 확정 근거는 없다.**
- 바깥 `0910` = **작업 폴더를 만든 날(MMDD)** 과 맞는다 — 0907 폴더 09-07 10:51 생성 ·
  0908 폴더 09-07 18:01 · 0910 폴더 09-10 10:31.
- 뒤 `260923` = **방영 예정일로 보인다** — 항상 폴더 날짜보다 뒤고 회차마다 다르다
  (0907→260908 · 0908→260910 · 0910→260923).
- **로컬에 더원 일정표가 없어 방영일임을 확인하지 못했다. 이정찬 확인이 필요하다.**

---

## 5. 다음

**5-1. 모름.** 새 회차 폴더가 없다(0907·0908·0910 뿐). 다음 회차·마감을 지시받은 것도 없다.

**5-2. 총괄에게 바라는 것**

1. **병합은 지금 해도 된다.** 겹치는 파일 0, srt_rules 회귀는 1-1 로 확인했다.
2. ~~결정이 필요한 것 셋~~ — **셋 다 내렸다 (이정찬, 15:10). 총괄이 처리할 것 없다.**
   - 숏폼 자막 0.19초 당김 재발행 · 롱폼 자막 정답본 확보 → **둘 다 지금은 안 중요하다.**
     차명11 도 더원 L08 도 더 편집하지 않는다. **새 채널(차트명가New) 콘텐츠로 넘어간다.**
     대기열에 쓸 작업이 없으니, 남는 것은 **.srt 를 얼마나 잘 뽑느냐는 기술**뿐이다.
     규칙·채점대는 그대로 두고, 정답 자료가 새로 생기면 그때 다시 잰다.
   - 더원 폴더 규칙(4-2) → **E 영역 밖.** 답은 4-2 에 적어 뒀으니 참고만 하고, 확정은 다른 쪽에서 한다.
3. **본류 문서 50개 갱신할 때 코드도 같이 봐 달라.** `tools/premiere/`(29개)와
   `tools/photoshop/config.json` 이 `C:/pprolab`·`C:/cmgwork` 를 박고 있는데 그 폴더가
   오늘 삭제됐다. 내 영역이 아니라 손대지 않았다. B 가 `labdir` 를 만들어 뒀다고 D 가 전했다.
4. **`tools/cutedit/prlinks.py` 를 전 파트 공용으로 쓰자.** 오늘 같은 사고가 두 번 났다 —
   둘 다 '담는 쪽(파일이 다 담겼나)'만 보고 '쓰는 쪽(누가 이 경로를 무는가)'을 안 본 것이다.
   옮기기 전 `find`, 옮긴 뒤 `check`. **검사 범위에서 목적지 폴더를 빼지 않는다.**
