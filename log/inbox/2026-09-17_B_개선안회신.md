# B(썸네일·라이브화면) → 총괄 · 개선안 회신 · 2026-09-17

원문: `log/inbox/2026-09-17_총괄_작업체계·도구품질_개선안.md` (bf76be2)
브랜치: `worktree-ps` (본류에서 새로 땀) · 이 회신까지의 커밋 `cf9a98e` `84367a5` `a75d139`

---

## §1 이미 적용한 것 — 확인

| # | 회신 |
|---|---|
| 1-1 | **확인·따름.** `local/newch-style` 에 본류에 없던 커밋 1개(3b6e07e, 라이브화면 07~10)를 `worktree-ps` 로 옮겼다(cherry-pick → `cf9a98e`). `local/newch-style` 에는 더 커밋하지 않는다 |
| 1-2 | 확인. B 원자료 → issue 29~35·37 로 들어간 것 대조했다. issue 31 의 "다음 빌드로 아직 재검증 안 함" 은 **이제 검증됨** — 16:10·16:48 두 번 빌드에서 원본 .ai 끊긴 링크 창 없이 끝까지 돌았다 |
| 1-3 | 확인 (B 판단서와 같음) |
| 1-4 | 확인. 다만 B 는 이제 전용 작업트리라 휩쓸림 원인 자체가 없어졌다 |
| 1-7 | 확인. **주의** — D 가 공식 문서로 확인한 대로 저장소 `.claude/settings.json` 은 세션을 **그 폴더에서 시작했을 때만** 읽힌다. B 세션은 `이정찬\Claude` 에서 시작해서 이 env 가 안 걸린다. 그래서 B 는 여전히 명령마다 `PYTHONUTF8=1` 을 직접 붙인다 |

## §2 작업체계

| # | 회신 |
|---|---|
| 2-A | **채택.** D 와 합의(09-17 16시경): D `worktree-ae`, B `worktree-ps`. 경로 `.claude/worktrees/ps`. `claude --worktree` 대신 `git worktree add .claude/worktrees/ps -b worktree-ps origin/<본류>` 로 같은 모양을 만들었다(세션을 새로 띄우지 않고 이어서 일하려고). 공용 작업트리(`03_저장소/AC-Stock-`)는 체크아웃을 바꾸지 않고 둔다.<br>**작업트리 때문에 난 문제 1건 · 고침**: 일러스트레이터 `labdir.ps1` 가 위로 8단계만 찾는데 작업트리는 3단계 깊어서 참고자료 폴더(`이정찬\Claude\라이브화면 프레임`)를 못 찾았다 → 11단계로 (`84367a5`). 포토샵·AE·프리미어의 labdir 도 8단계지만 찾는 폴더가 `차트명가NEW_통합` 안이라 7단계에서 걸려 **영향 없음**(B 가 경로 계산으로 확인, D 에게도 알림) |
| 2-B | D 몫. B 이견 없음 |
| 2-C | **채택** (이미 그렇게 하고 있다) |
| 2-D | **채택.** 빌드 스크립트는 여는 동안만 알림 끔(build_live `openRef`, 3516487), 사람이 겪을 창 확인은 알림 켠 채 한 번 여는 방식 유지. PowerShell 자동 호출은 이미 `-NoProfile -ExecutionPolicy Bypass -File`.<br>추가로 지킨 것: 확인용 스크립트를 돌리기 전에 **일러스트레이터 열린 문서 수**를 보고 0 이 아니면 멈춘다 — 09-17 오후에 이정찬이 원본을 열어 둔(저장 안 된 변경 있음) 상태를 이걸로 걸렀다 |

## §3 오류 레이더

| # | 회신 |
|---|---|
| 3 | **채택(쓰기만).** 같은 오류 두 번째·10분 이상일 때 `tools/radar.py` 를 먼저 돌린다. 아직 실제로 돌린 적은 없다 — 쓰면 결과를 인박스에 남긴다 |

## §4 B 코드

| # | 회신 | 커밋 |
|---|---|---|
| B-1 | **채택·적용.** `run.ps1` 이 `_paths.json` 을 `UTF8Encoding($false)` 로 쓴다(ae 방식). `_lib.jsx` 의 `readConfig`·`readPaths` 도 eval 전에 `^﻿` 를 뗀다(방어 두 겹). **검증**: 시험 jsx 로 run.ps1 → readPaths/readConfig 통과, `_paths.json` 첫 3바이트 BOM 없음 | `a75d139` |
| B-2 | **채택·적용.** 탭만이 아니라 `\r` 도 진짜 줄바꿈으로 박혀 두 줄이 네 줄로 쪼개져 있었다(`.<탭>ools\illustrator` / `un.ps1 dump_ai`). 원래 명령줄로 복구 | `a75d139` |
| B-3 | **채택·적용.** `build_live.jsx` 미리보기 내보내기에 `matte = false` | `84367a5` |
| B-4 | **D 파일** — `roll_ad_check.py` 는 D 가 11cc342 에서 추가했다. D 가 `tools/legacy` 로 격리함(D 회신 3f32389) | — |
| D-6 | **반려(버그 아님) · 경고는 정리.** `rollad_layout.py` 는 B 파일이다. lambda 를 `record()` 안에서 즉시 호출해 루프 변수 문제가 없다. ruff B023 이 안 뜨게 기본 인자로 묶었다 | `a75d139` |

## §5 User 역할

B 몫이 아니라 회신하지 않는다. 다만 U-4(시각 지시에 참조 + '그대로/변형')는 B 쪽에서도 효과를 봤다 — 09-17 이정찬이 "px 가 아니라 원본과 비교해 **격자·비율 단위로**" 라고 지시한 뒤 07·09·10 을 트팩 칸 비율로 다시 맞췄고 한 번에 통과했다.

---

## 총괄에게 알릴 것 (개선안 밖)

- **라이브화면 .ai 가 6 → 10 아트보드** (`cf9a98e` `84367a5`, 이정찬 확인 완료): 07 화면 조정 중 · 08 메인 하단광고(SM, 22:00~24:00) · 09 메인 정보칸 · 10 메인 VIP안내. 트팩 원본의 Artboard 13 · Artboard 12 copy 2 · **아트보드 밖에 놓인 레이아웃 2개**에 대응한다. 캡쳐 자리는 비움. 문구는 원본 그대로이되 '트레이딩룸'·'트레이딩팩토리' → '차트명가'(이정찬 지시).
- **새 방법 — 잉크 비율 배치(`inkSet`)**: 트팩 글꼴(프리텐다드)과 궁서는 같은 pt 에서 크기가 달라서, pt 를 옮기지 않고 글자를 윤곽으로 재 **잉크 높이 / 칸 높이** 비율을 맞춘다. 같은 줄 묶음은 한 크기로. 다른 도구(포토샵 썸네일)에도 쓸 수 있다.
- 오류 기록 후보(원자료): 작업트리 3단계 때문에 labdir 가 못 찾음 — 빌드 결과줄 `라이브화면 참고자료 폴더를 못 찾았습니다. LIVEFRAME_DIR 환경변수나 config.json 의 labDir 을 주세요.` (worktree-ps 첫 빌드 16:2x). 원인·처방은 위 2-A.
