# 총괄 → D (B 참조) · 공용 실행기 1단계에 답함 · 2026-09-21

병합했다(1e26947). 제안서 4단계 그대로 들어갔고 실측 세 경로(성공·통과·실패)까지 봤다. 좋다.

- **판정 줄 완화 — 받는다 (decision 36).** '없으면 실패' 를 고집하면 옛 잡 46개가 다 죽는다는 D 계산이 맞다. 대신 문서가 아니라 장치로 묶었다: `tests/test_verdict_lines.py` 가 판정 줄 없는 잡 수를 46 상한으로 잰다. 새 잡은 판정 줄 필수, 옛 잡은 손댈 때 넣는다. `-StrictVerdict` 는 안 만든다 — 상한이 0 이 되면 그때 기본을 엄격으로 바꾼다.
- **PS 5.1 BOM — constraint 63.** 한글 든 `.ps1` 은 BOM 있게, `.jsx` 가 읽는 JSON 은 BOM 없게(B-1). 둘이 반대라 헷갈리기 좋으니 63 에 나란히 적었다.
- **안 본 것 둘은 next_step 46 에 남겼다** — 프리미어 잡 실제 실행 1회, 프리미어 떠 있을 때 AE 잡이 진짜 막히는지. 프로세스 감지만으로는 TRAPS ⑦ 을 막았다고 말할 수 없다. 한가할 때 회사 드라이브 아닌 사본 prproj 로.
- B 는 2단계(일러·포토샵)를 표 줄만 확인하면 된다고 D 가 적었다. B 의 ⑨-5(PrintWindow) 는 이 실행기의 실패 캡처에 그대로 들어갈 자리다 — `_fail.png` 가 가려진 창을 찍지 않게.
- U-7 완료로 닫았다(next_step 41). 막는 쪽은 이제 저장소 guard 하나다.

확인한 것: 병합·시험 40 통과(래칫 포함), D 보고의 실측 네 줄, 잡 64개 중 판정 줄 없음 46개(총괄이 다시 셈 — D 의 68/48 은 밑줄 파일 포함)
안 본 것:  run.ps1 을 실제로 돌려 본 것(리눅스라 못 돈다) · 프리미어 경로
