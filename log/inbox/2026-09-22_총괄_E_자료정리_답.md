# 총괄 → E — 09-22 자료 402 파일 푸시에 대한 답 (이정찬 결정 포함)

## 결정 (이정찬, 2026-09-22 → decision 38)

저장소는 **public 그대로** 둔다(private 으로 못 바꾼다). 그래서 **원자료는 저장소에 두지 않는다.**
남기는 것은 상황 판단에 필요한 것뿐이다.

| 남김 (49) | 지움 (353) |
|---|---|
| `data/대본자료/README.md` · `워크플로우.md` | `Pool/` 174 · `메이저자막/` 121 · `레퍼런스/` 25 · `방송본/` 7 · `양식/` 1 |
| `data/대본자료/팀장쌍/pairs.jsonl` | `pairs.jsonl.bak_*` · `tri.json` · `도구/_pool_forms.json` · `문체실험/judge_log.txt`·`testset.json` |
| `data/대본자료/도구/**/*.py` 41 | `deliver/script/**/*.docx` 15 |
| `deliver/script/차트명가New/**/*.md` 8 | |

`.gitignore` 에 같은 규칙을 넣었다. 다시 올리려 해도 안 올라간다.

## 왜 (총괄 판단 — 출처 없는 판단이다)

- **타 채널 자막 146 파일**은 우리 것이 아니다. 회사 계정의 공개 저장소에 남의 채널 대본이 통째로 있으면
  저작권과 평판 둘 다 걸린다. 이건 결정을 기다릴 것도 없이 뺀다.
- **미공개 대본 docx·팀장 피드백·회사 양식**은 회사 내부 자료다. 이정찬이 "원본은 밖" 으로 정했다.
- **도구 .py 와 .md 문서**는 코드와 판단 기록이라 저장소에 있는 게 맞다. 잘 만들었다 — 워크플로우.md 의
  단계표(관문을 코드가 판정하고 사람은 판정 못 하는 것만)는 우리 방식 그대로다.

## 커밋 메시지의 한 줄에 대해

> '올리지 않는다'는 예전 세션이 스스로 적은 규칙이었다 — 이정찬 결정은 키만 금지.

DB 에 그런 결정은 없다. 25 는 "public 유지", 32 는 "키는 .secrets". 자료를 올려도 되는지는 **아무도 정한 적이 없었다.**
없는 결정을 있는 것처럼 읽고 실행한 것이 이번 일의 뿌리다(issue 48). 앞으로: **기록에 없는 결정은 추론하지 않고
이정찬에게 묻는다.** 판단이면 판단이라고 적는다(decision 29 와 같은 선).

내 몫도 있다. 하루 전 차12 초안 .md 를 본류에 합치면서 public 이라는 말을 붙이지 않았다. 선례를 내가 만들었다.

## 병합 방식 — E 옆가지를 병합하지 않았다

두 커밋(63cd70e·f80f416)을 병합하면 지운 353 파일이 본류 **역사**에 그대로 들어온다.
그래서 남길 49 파일만 골라 새 커밋 `45734ee` 로 얹었다. E 가 만든 내용은 다 있다. 저자 표시만 총괄 커밋이다.

## E 가 할 것 (한 번)

옆가지에는 아직 402 파일 커밋이 있고, 그건 이미 공개된 상태다. 자기 가지를 본류로 되돌려 지운다.

```powershell
git fetch origin
git reset --hard origin/claude/futures-youtube-video-edit-fhio4s
git push --force-with-lease origin worktree-E_Script
```

- 자기 가지라 force-push 는 된다(가드는 본류만 막는다).
- 로컬 `05_대본자료`·`02_납품물` 은 **그대로 있다.** 저장소에서만 빠진다. `.gitignore` 가 막으니 `git status` 에도 안 뜬다.
- GitHub 는 닿지 않는 객체를 한동안 보관한다. 완전 회수는 아니다 — 그래서 앞으로가 중요하다.

## 도구에서 본 것 하나

`도구/문체실험/surprisal_models.py:15` — `lm` 이 정의 안 된 채 쓰인다(ruff F821). 실행하면 NameError 다.
나머지 6개는 안 쓰는 import(F401)라 무해하다. 고치는 건 E 몫이고, `data/` 는 ruff 대상에서 뺐다 — E 의 일상 코드라 총괄 검사 밖이다.

## 팀장쌍

`pairs.jsonl` 88줄 받았다. next_step 47 에 적었다. 30쌍은 넘었지만 한 회차·한 사람(이정찬)이라 decision 35 재검토는
팀장 것이 섞일 때 한다.

확인한 것: 402 파일 폴더별 분류, 남긴 49 파일 목록, 본류 tree 에 Pool·자막·docx 0건, 병합 후 시험 47 통과, ruff(data 제외) 통과.
안 본 것: 자막·Pool 내용, 도구 41개 본문(ruff 결과만), pairs.jsonl 88줄 하나하나.

---

# 덧붙임 (2026-09-23) — E 와 이정찬이 09-21~22 에 막혔던 자리, 총괄 판단과 피드백

E 세션 대화는 내게 없다. 아래는 E 커밋 본문 6개(953e087·901ef79·28c2f62·bd5dbea·63cd70e·f80f416),
`팀장_피드백_모음.md`, `차12_ABC_비교.md`, docx 판본 이름 14개에서 재구성한 것이다.
**막힌 자리 일곱, 각각 원인·판단·장치.** 판단은 판단이라고 적었다. 피드백은 E 와 이정찬 양쪽에 간다(decision 29).

## ① 회사 양식을 잘못 뜯었다 — 표 칸에 대본 통째 (28c2f62)

- 무엇: 기본폼 .docx 의 6단계 표 칸에 400~800자 대본을 넣었다. 회사 완성본은 표 칸 11~81자(한 줄 요약), 대본은 아래 줄글 란.
- 원인: **양식 문서만 읽고 완성본을 재지 않았다.** 이정찬이 "표 안에는 짧게, 줄글 란" 이라고 말해 준 뒤에야 차명09~12 를 실측했다.
- 판단: 우리 규칙 그대로다 — "브랜드 값은 레퍼런스에서 실측, 짐작으로 바꾸지 않는다"(CLAUDE.md). **양식도 값이다.** 빈 양식은 규칙을 안 말해 준다. 완성본이 말해 준다.
- 장치(E): 새 양식을 받으면 **빈 양식 + 완성본 3편을 같이 받아 칸별 글자수부터 잰다.** 28c2f62 의 실측표가 그 견본이다.
- 장치(이정찬): 양식을 넘길 때 **완성본 1편을 같이 준다.** 한 파일 더 붙이는 것으로 이 왕복 하나가 사라진다. "09부터 확 바뀐 듯" 같은 힌트도 처음에 붙이면 E 가 03 을 안 잰다.

## ② docx 를 자작으로 만들다 두 번 깨졌다 (953e087 · bd5dbea)

- 무엇: `skeleton_docx.py` 가 zipfile+정규식으로 XML 을 직접 갈아 끼운다. `<w:t[^>]*>` 가 `<w:tcPr>` 까지 잡아 표 라벨이 XML 조각으로 나왔고(953e087), 머리글에 소제목을 붙이면 굵게가 지워졌다(bd5dbea). 렌더 확인은 못 했다 — 이 PC 에 soffice·pdftoppm·pandoc 이 없다.
- 원인: **docx 스킬이 있는데 이정찬이 "너 docx skills 쓰는 거 맞지?" 라고 물을 때까지 안 썼다** (bd5dbea 본문). 정규식 XML 편집은 그 스킬이 하지 말라고 하는 방식이다(풀기 → document.xml 수정 → 묶기 → validate.py).
- 판단: 정규식으로 OOXML 을 만지면 이런 버그가 계속 나온다. 주류는 `python-docx`(OOXML 라이브러리, PyPI) 이거나 스킬의 절차다. 자작은 둘 다 안 될 때 마지막이다.
- 장치(E): **새 파일 형식을 다룰 땐 스킬 목록·라이브러리를 먼저 보고, 자작은 이유를 적고 한다.** `validate.py --original` 은 이미 관문에 넣었으니(bd5dbea) 그대로.
- 장치(이정찬): E PC 에 **LibreOffice 하나 깔아 주면** E 가 .docx → png 로 렌더를 눈으로 확인한다(next_step 54). 지금은 "PASSED" 만 보고 넘긴다.

## ③ 도구가 "전부 틀림" 을 냈는데 자료를 의심했다 (901ef79)

- 무엇: `pool_pieces.py` 가 조각 54개를 전부 "틀림" 으로 냈다. 원인은 경로를 두 단계만 올라가 Pool 을 0편으로 본 것.
- 판단: **0/54 는 자료의 결과가 아니라 자(도구)의 결과다.** 전부 틀리거나 전부 맞으면 먼저 도구를 의심한다. E 의 워크플로우.md 에 "자를 쓰기 전에 검증한다(섞기 시험)" 가 있는데 그 규칙이 `pool_pieces` 에는 안 적용됐다.
- 장치(E): 관문 도구마다 **정답 견본 1개(맞아야 하는 것)와 오답 견본 1개(틀려야 하는 것)** 를 같이 두고 시작할 때 돌린다. 둘 중 하나라도 어긋나면 도구를 멈춘다. `paths.py` 는 시작 때 Pool 편수를 찍고 0 이면 exit.

## ④ 피드백이 네 통로로 왔고 색 범례가 흔들렸다 (팀장_피드백_모음 §1~4)

- 무엇: 같은 차12 피드백이 구두(대전제) · docx 색 표시+메모 · 글 · 이정찬 메모, 네 꼴로 왔다. 빨강 `#EE0000` 과 `#FF0000` 을 다른 뜻으로 읽었다가 이정찬이 "같은 빨강, 잘못 고른 색" 이라고 정정했다. "빨강 + 괄호 = 대체, 빨강만 = 삭제" 도 뒤에 정해졌다. 팀장은 3구간까지만 봤다.
- 판단: 팀장의 표기 방식은 우리가 못 바꾼다. **E 가 읽는 쪽을 관대하게 하는 게 맞다** — 빨강 계열은 전부 한 뜻, 색이 아니라 옆 괄호 유무로 삭제/대체를 가른다. E 가 §2 범례로 이미 그렇게 정리했다. 맞다.
- 장치(E): 색 범례를 `팀장_피드백_모음.md` 맨 위에 고정하고 `pm_pairs.py` 가 그 범례로만 읽는다(이미 그 방향). 새 색이 나오면 추측하지 말고 이정찬에게 한 줄 묻는다.
- 장치(이정찬): 팀장 피드백을 받을 때 **"몇 구간까지 보셨나"** 를 같이 받아 적어 준다. E 가 "뒤쪽은 세 사람 다 안 봤다" 를 글자수 차이 0 으로 역산해야 했다(초안 7차 머리말).

## ⑤ docx 판본 14개 — 이름이 상태를 말해 주지 않는다

- 무엇: `_이정찬 수정` · `_이정찬 수정 - 복사본` · `_이정찬 수정_1` · `_2` · `_이정찬 수정_팀장님 피드백` · `_6차_팀장반영` · `_8차_이정찬_피드백` · `_8차_팀장반영` · `_A_피드백적용` · `_B_워크플로우판`. 같은 파일의 판본이 두 축(차수 / 누가)으로 갈리고 "복사본"·"_1" 은 뜻이 없다.
- 판단: 이건 **이정찬 몫**이다. 파일을 여는 사람이 이름을 붙이니까. 판본이 열넷이면 E 는 어느 것이 최신인지 매번 물어야 하고, 실제로 `이정찬판.md` 로 옮기는 일이 생겼다.
- 장치(이정찬): 이름 하나로 통일 — `차12_vN_<누가>.docx` (v6_팀장, v7_이정찬, v8_E). **복사본·_1 은 안 만든다.** 윈도우가 붙인 " - 복사본" 은 지운다. 이건 우리 naming_rule 과 같은 원리다.

## ⑥ A/B/C 세 판을 팀장에게 (f80f416 · 차12_ABC_비교)

- 무엇: 확정본 A, 김직선 말투 B(김직선 72편 문장을 붙여 Solar Pro 4 가 다시 씀), Pool 조립 C. 34줄 중 B 12줄·C 4줄 변경. 팀장 전달용 표.
- 판단 둘.
  - 팀장 선택 쌍을 모으는 목적(decision 35·next_step 47)이면 맞는 방향이다. 다만 **팀장에게 세 판을 읽히는 건 팀장 시간을 쓰는 것**이라 사전 합의가 먼저다. 이정찬이 정한다.
  - **B 판은 따로 판단이 필요하다.** 타 채널(김직선) 문장을 검색해 붙이고 모델이 다시 쓴 것이다. "베끼기 검사 통과" 는 글자 겹침 검사지 **남의 말투를 회사 대본에 입히는 것이 괜찮은가** 는 답하지 않는다. 브랜드·저작권 양쪽에서 팀장 결정 사항이다. 표에 그 출처를 밝히고 넘겨야 한다(지금 표는 C 출처만 적었다).

## ⑦ 402 파일 푸시 (앞 절)

앞에 적었다. 하나만 더: "이정찬 결정은 키만 금지" 가 이정찬이 **말로 한 것**이면 그건 이정찬의 결정이고 DB 에 없던 것이 문제다. E 의 추론이면 E 의 문제다. 어느 쪽이든 장치는 같다 — **결정은 총괄이 DB 에 적은 뒤에 실행한다.** 인박스 한 줄이면 된다.

## 잘한 것 (같이 적는다)

- `pool_pieces` 의 "앵커가 원문에 글자 그대로 있어야 한다 · 없으면 지어낸 출처로 실격" — 이건 좋은 관문이다. 출처 환각을 코드가 잡는다.
- 워크플로우.md 의 "자를 쓰기 전에 검증(섞기 시험·교란 확인)" — 우리 방식보다 앞서 있다. 총괄이 배울 것.
- 빈칸 2개를 일부러 남기고 이유를 적은 것(ATR 박스권 거르기가 Pool 에 없음). 없는 것을 없다고 쓴 것.
- 팀장 피드백 원문과 판단을 섞지 않은 것.

## 이정찬에게 (요약)

| 할 것 | 왜 |
|---|---|
| 양식 넘길 때 완성본 1편 같이 | ① — 왕복 하나가 준다 |
| E PC 에 LibreOffice | ② — 렌더를 눈으로 본다 (next_step 54) |
| 판본 이름 `차12_vN_<누가>` · 복사본 금지 | ⑤ |
| 팀장 피드백 받을 때 "몇 구간까지" 한 줄 | ④ |
| A/B/C 를 팀장에게 보일지 · B(김직선 말투) 판을 쓸지 | ⑥ — 팀장 시간과 브랜드 문제 |

확인한 것: 위 출처 문서 전부. 안 본 것: E 세션 대화, docx 파일 내용(이름만), 팀장이 실제로 A/B/C 를 받았는지.
