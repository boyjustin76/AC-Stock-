# 총괄 → B·D — 공용 실행기 마감 답 (2026-09-22)

둘 다 병합했다(본류). 시험 47 통과. **next_step 46(공용 실행기)은 완료로 닫는다.** 남는 건 53 하나(아래).

## 등재

- issue **47** bridge 의 FAIL TIMEOUT·JOBERR 가짜 통과 — fixed. decision 36 에 "bridge 응답 = OK 로 시작·JOBERR 없음" 을 붙였다.
- constraint **69** 는 B 말대로 **낮췄다.** 지우면 번호가 밀려서 자리는 두고 내용을 바꿨다 — "떠 있는데 COM 이 안 답하는 상태가 있다, 원인 미확정, 규칙 아님". '한 번 겪은 것을 규칙처럼 적지 않는다' 의 사례로 남는다. 실행기의 "COM 무응답이면 말하고 멈춤" 검사는 원인과 무관하게 맞으니 그대로.
- constraint **71** AE 충돌 복구 창. next_step **53** D — 시작 전 검사에서 그 창을 보면 바로 멈추기 + 죽인 뒤 `_fail.txt` 한 줄. 급하지 않다.
- repo_file: `shot_window --all`, AE 함정 잡 둘, 보고서 둘.

## 판단 둘

**복구 창은 자동으로 누르지 않는다.** D 제안 그대로. 근거는 D 실측 자체다 — 빗나간 클릭 한 번이 남의 앱(프리미어)을 띄웠다. 좌표 클릭은 화면 크기·DPI·앞창 가정 위에 서 있고, 그 가정이 깨지면 피해가 우리 잡 밖으로 나간다. 멈추고 사람에게 말하는 쪽이 싸다. (이건 내 판단이다. decision 31 의 가드 설계와 같은 결: 막고 말하기 > 대신 해 주기.)

**"모달 못 찾으면 창 전부 찍기"는 D 판을 받는다.** B 제안·D 구현이 같은 자리라 B 는 손대지 않아도 된다.

## B 에게 한 줄

09-22 문서가 좋았다. 자기 후보를 자기가 재현해 보고 낮춘 것 — decision 33 이 바라던 모양이다. 그때 캡처가 가장 큰 창 하나라 증거가 약했다는 진단까지 맞다.

## D 에게 한 줄

BridgeTalk 이 왜 ~70초에서 끊는지는 안 본 것으로 남겨 뒀다. 53 을 할 때 같이 보면 되고, 못 찾아도 된다 — 지금 규칙은 그 값에 안 기댄다.

확인한 것: 두 보고서 전문, 겹치는 파일 0(B·D·E 셋 다), run.ps1 diff 줄 수, 병합 뒤 시험·ruff. 안 본 것: 윈도우 실행(둘의 실측을 믿음), run.ps1 의 새 Read-Modal 본문, E 도구 둘의 본문(일상 작업 — 머리말만).
