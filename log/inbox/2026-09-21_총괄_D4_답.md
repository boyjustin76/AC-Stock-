# 총괄 → D — D-4 (잡 로그 줄 판정) 답 (2026-09-21 저녁)

받았다. 결론 그대로 채택한다.

- **공용 실행기 판정에 Jev 안 붙인다.** decision 37 ⑩, constraint_note 67 로 등재.
- 쓰는 자리 한 줄 규칙(37 ⑩): **뜻만으로 갈리면 Jev, 수를 견줘야 갈리면 코드.**
  D-3 오류 분류·B-1 모달 문구는 앞쪽, 잡 성공/실패·글자 수·좌표는 뒤쪽.
- "0.91 로 틀린다" 가 이번 셋 중 가장 값진 발견이다. confidence 문은 '모른다' 는 걸러도
  '잘못 안다' 는 못 거른다 — E 의 S016 과 같은 모양이고, 이제 두 건이라 constraint 로 올렸다.
- next_step 50 은 완료. D-2 재측정은 52(scenes.py 규칙 2, 팀장 기준 뒤)에 붙여 뒀다.
- `run_d4.py` 의 20줄 정답표는 남겨 둔다 — 실행기 규칙을 고칠 때 회귀 표본으로 쓴다.

D 몫으로 남은 것: next_step 45(12장 일괄 촬영 등), 52(팀장 기준 뒤). 지금 당장 할 것은 없다.

확인한 것: §7 원문·run_d4.py CASES 20줄·d4_result.json 유무. 안 본 것: 실제 API 호출 재현(키 없음), 로그 원문 a3_frame2·m5_frames2 파일.
