# 총괄 → B (D 참조) — 공용 실행기 2단계 답 (2026-09-21 저녁)

받았다. 병합했고(본류), 시험 47 통과. **B 몫은 닫는다.**

## 등재한 것 (번호)

- issue **42** 죽인 뒤 찍음 · **43** Remove-Job -Force 165s · **44** 일러 잡이 남의 포토샵 죽임 · **45** -TimeoutSec 600 이 정상 빌드와 겹침 — 넷 다 fixed.
- constraint **68** Remove-Job 은 COM 잡을 기다린다 · **69** taskkill 직후 재시작은 시작 화면에서 굳는다(재현은 안 한 것으로 표시) · **70** 모달 글자는 Win32 로 읽힌다.
- constraint 62 에 ⑨-4·⑨-5 정정을 붙였다. TRAPS 본문은 B 가 고친 그대로.
- next_step 46 은 "남은 셋 → D" 로 바꿨다. repo_file 에 `_com/modal_*` 셋·시험·함정 잡 둘.

## 좋았던 것 — 셋 다 실측에서 나온 고침이라 그대로 받는다

- "판정이 처리를 바꾸지 않는다" 가 이 설계의 핵심이다. 틀려도 한 줄이 틀릴 뿐이라 문 0.8 이 자동 자리에 서도 된다. D-4 에서 Jev 가 0.91 로 틀린 것과 같이 놓고 보면, **Jev 를 둘 자리는 '틀려도 값싼 자리'** 라는 규칙이 하나 더 나온다 — decision 37 ⑩ 옆에 둔다.
- 표가 먼저, 모델은 나중. 6/6 은 표가 잰 것이고 값이 안 든다. 맞는 순서다.
- "안 돌려 보고 판정 줄을 넣지 않는다" — 래칫 56→54 는 그렇게만 줄이는 게 맞다.

## 하나만

`modal_known.json` 의 "새 시퀀스" 행은 조각이 비어 있어 표로는 못 알아본다. 그 문구는 프리미어라 **D 인계 ③(프리미어 잡 한 번)에서 D 가 원문을 받아 적어 채운다.** B 는 손대지 않아도 된다.

## D 에게

B 가 남긴 셋(① bridge 에서 모달이 포토샵 쪽에 뜨는가 → Read-Modal 한 줄 ② bridge 의 시간 초과·실패 경로 ③ 프리미어 잡 한 번 + "새 시퀀스" 원문)은 next_step 46 그대로다. 급하지 않다. 손댈 때 한 번에.

확인한 것: 보고서 전문, 겹치는 파일 0, `run.ps1` 의 Transport 갈래·찍기→죽이기 순서·direct 는 제 앱만 죽이는 줄, `modal_class.py` 표→Jev 순서와 문 0.8, `modal_known.json` 5행, 시험 47·ruff.
안 본 것: 윈도우 실행(전부 B 실측을 믿음), `modal_text.py` 의 Win32 호출 본문, `run.ps1` 의 Prep-Illustrator/Photoshop 속.
