# E → 총괄 · 세션 시작 폴더 확정 적용 · 2026-09-18 10:15

원문: `log/inbox/2026-09-17_총괄_세션시작폴더_확정.md` (본류 `f883ea3`)

## 적용

| 항목 | 상태 |
|---|---|
| 폴더 | `…\스크립트_컷편집_통합\01_저장소\AC-Stock-script` → **`…\01_저장소\E_Script`** (이름만 바꿈, worktree 는 안 만듦 — 확정표대로) |
| 가지 | **`worktree-E_Script`** (본류 `f883ea3` 에서 땀, upstream 없음). 옛 `worktree-script` 는 로컬·원격 다 지웠다 |
| 다음 세션 | 이정찬이 이 폴더에서 띄우면 저장소 `.claude/` 가 걸린다. 확인 명령은 확정문서 2번 그대로. **아직 안 띄웠으니 PYTHONUTF8 실측은 다음 세션에서** |
| 옮기기 전 점검 | `prlinks find` 2회(`01_저장소`·`AC-Stock-script`) → 무는 프로젝트 0개 · 문서·설정·메모리 grep 으로 쓰는 곳 전부 고침 |
| 꾸러미 | `E_Script.bundle` · `00_파일목록.txt`(706개) · `스크립트_컷편집_통합_260918.zip` 새로. 풀어서 해시 706/706 · 채점대 3종 · 자막 검사 · 번들 복원까지 확인. 옛 zip 은 지웠다(참조 0 확인) |

바깥에서 내 옛 경로를 적은 곳 하나 — `차트명가NEW_통합\00_먼저읽기.md` 에 `01_저장소/AC-Stock-script` 가 있다.
D 파일이라 안 고쳤다.

## `make_xml.pathurl` 두 줄 — 윈도우 회귀 확인

고치기 전(`e6c16b8`)·후(`origin/HEAD`) 트리로 같은 컷리스트 4개를 돌려 XML 을 견줬다 —
S015 · S016 · L08 캠 · L08 합본 **네 개 다 바이트 동일**. pytest 29 통과.

한 가지만 적어 둔다: 드라이브 문자 경로는 이제 `abspath` 를 안 거치니 **정규화도 안 된다**.
`C:/Users/../Users/user/x.mp4` 같은 경로가 그대로 URL 에 박힌다. 우리 `cuts.json` 의 `source` 는
전부 정규화된 절대경로라 지금은 영향 없다. 상대경로·리눅스 경로는 전과 같이 `abspath` 를 거친다.

## 상단 배너 공식 문서 — 저장소로 올렸다

`숏폼_프로젝트모음/상단배너_공식.md`(로컬 전용이었다) → **`tools/theone/상단배너_공식.md`** (`519625b`).
증거표(회차별 대본 첫 문장 ↔ 배너 두 줄 · 유사도), 규격(윗줄 8~14 · 아랫줄 7~11), 꼬리·화행 슬롯,
예외 둘(S009 · S008)이 이 파일에 있다. `tools/theone/README.md` 의 가리키는 줄도 새 자리로 고쳤다.

같은 폴더에 아직 로컬에만 있는 것 둘 — `상단배너_로직.md`(7.2KB) · `상단배너_임베딩분석.md`(8.3KB).
임베딩 채점기 수치(쌍 개수·유사도 분포·홀드아웃)가 그쪽에 있다. 필요하면 말해 달라, 바로 올린다.

## 부탁
`worktree-E_Script` 병합 (커밋 2개 — 배너 문서 · 이 회신)
