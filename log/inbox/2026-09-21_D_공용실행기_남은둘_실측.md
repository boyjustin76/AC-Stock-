# D → 총괄 · 공용 실행기 — 안 봤던 둘을 봤다 · 2026-09-21 낮

`2026-09-21_총괄_공용실행기_1단계_답.md` 에서 next_step 46 에 남겨 둔 둘을 실측했다. **둘 다 된다.**
회사 드라이브 `.prproj` 는 열지 않았다 — `probe.jsx` 가 아무것도 열지 않고 읽기만 하는 잡이라 그걸 썼다.

## 1. 프리미어 잡 실제 실행 — 된다

```
.\tools\premiere\run.ps1 -Job probe -TimeoutSec 280
  Adobe Premiere Pro 안 떠 있음 — BridgeTalk 이 띄웁니다
  bridge: OK probe ok, 25 lines
  걸린 시간: 16.7s
  통과(판정 줄 없음) — 옛 잡이다     종료코드 0
```

이어서 `save_quit` 로 닫았고, 이쪽은 **엄격 경로**가 걸렸다.

```
  성공 — save_quit.txt: 판정	전부 저장 · app.quit() 호출     종료코드 0
  → 프리미어 종료 확인
```

즉 프리미어도 AE 와 똑같이 **경고 통과 · 엄격 성공 · 정상 종료** 세 가지가 다 돈다.
탭으로 구분된 `판정\t…` 도 그대로 잡힌다(AE `c8q` 와 같은 모양).

## 2. 프리미어가 떠 있을 때 AE 잡 차단 — 진짜로 막힌다

프리미어를 띄워 둔 상태에서 AE 잡을 불렀다.

```
프리미어 떠 있음 (PID 10452)
.\tools\ae\run.ps1 -Job a1_smoke
  → throw: 프리미어가 떠 있습니다(PID 10452). AE 잡이 프리미어의 AE 엔진으로 갑니다(TRAPS 7).
            프리미어를 닫고 다시 부르세요.
  종료코드 1 · AE 안 뜸 (차단 성공)
```

앞서는 프로세스 감지만 봤는데, 이번엔 **실제로 잡이 멈추고 AE 가 뜨지도 않는 것**까지 봤다.
TRAPS ⑦ 을 막았다고 말해도 되는 상태다.

## 3. 같이 고친 것 — 실패 캡처가 가려진 창을 못 찍던 것 (총괄 지적)

`_fail.png` 가 화면 전체(`CopyFromScreen`)라 모달이 다른 창 뒤면 쓸모가 없었다.
`PrintWindow(PW_RENDERFULLCONTENT)` 로 **대상 앱 창만** 찍게 바꿨다. 구현은 한 벌만 둔다 —
`tools/_com/shot_window.py` (파이썬 쪽에 이미 검증된 코드가 있어서 그리로 모았다).

- 프로세스 이름을 주면 그 프로세스의 **보이는 최상위 창 중 가장 큰 것**을 고른다.
  `MainWindowHandle` 은 못 믿는다 — 일러스트레이터가 **160×28** 짜리 엉뚱한 창을 돌려줬다(실측).
- 창을 못 찾으면(최소화·앱 안 뜸) 종료코드 2 로 빠지고, 실행기가 화면 전체로 물러선다.
- 실측: 보이는 창을 줬을 때 `PrintWindow=1 958x1030`, 색 707가지(빈 그림 아님). 최소화된 앱은 못 찾고 물러섬까지 확인.

PowerShell 에서 직접 P/Invoke 로도 해 봤는데 `Add-Type -MemberDefinition` 이 `System.Drawing.Rectangle`
참조를 못 찾아 컴파일이 깨졌다. 구조체를 직접 정의하면 되지만, 같은 코드가 두 벌이 되는 게 싫어 파이썬으로 모았다.

## 남은 것

- 옛 잡 46개의 판정 줄 — 손댈 때 하나씩. 래칫(`tests/test_verdict_lines.py`)이 늘어나는 것만 막아 준다.
- B 몫 2단계(일러·포토샵). 표에 줄은 있고, 실패 캡처는 위 `shot_window.py` 를 그대로 부르면 된다.

---

확인한 것: 프리미어 `probe`·`save_quit` 실제 실행(16.7s·0.7s, 종료코드 0), 프리미어 정상 종료, 프리미어 떠 있을 때 AE 잡 차단(AE 안 뜸), 창 캡처 실측 2건, 세 스크립트 문법 통과
안 본 것:  실패 경로에서 **실제 모달이 떠 있을 때** `_fail.png` 가 그 모달을 담는지 (모달을 일부러 띄우지 않았다) · 포토샵·일러 경로
