# D → 총괄 · next_step 53 — AE 충돌 복구 창을 보면 바로 멈춘다 · 2026-09-22

총괄 답(`2026-09-22_총괄_실행기_마감_답.md`)의 53 을 했다. 누르지 않는다는 판단 그대로.

## 알아보는 법 (`tools/_com/modal_text.py --ae-recovery`)

어도비가 그린 창이라 문구는 안 읽히지만 **뼈대**는 읽힌다 (09-22 실측 두 벌).

| | 클래스 | 자식 창 | AE 주창 |
|---|---|---|---|
| **복구 창** | `#32770` | OS_ViewContainer ×2 + **OS_EditTextContainer** | 없음 |
| 시작 화면 | `#32770` | OS_ViewContainer 하나 | 없음 |

크기(534×365 / 766×516)는 화면 배율마다 달라질 수 있어 조건에 안 넣었다.

## 실행기에서 부르는 자리 (`tools/_com/run.ps1`, AE 만)

- **시작 전** — AE 가 떠 있고 복구 창이면 바로 멈춘다.
- **기다리는 동안 5초마다** — 보통은 AE 가 꺼져 있다가 COM 이 띄울 때 이 창이 뜬다. 시작 전 검사로는 못 본다.
- 멈출 때 **아무것도 죽이지 않는다.** AE 를 죽이면 다음에 또 뜬다. 포토샵의 bridge.jsx 는 BridgeTalk 이 끊으면 스스로 끝난다.
  붙잡힌 잡은 치우지 않고 나간다(Remove-Job -Force 가 165초 걸린 적 있음 — B 실측).
- 실행기가 AE 를 강제로 닫은 실패에는 `_fail.txt` 에 "다음 실행 때 충돌 복구 창이 뜬다 — '계속' 을 눌러야 한다" 한 줄.

## 잰 것

| 경우 | 전 | 후 |
|---|---|---|
| AE 를 강제로 닫은 실패 (`_trap_alert`) | 줄 없음 | 71초 · `_fail.txt` 에 주의 줄. 시작 화면을 복구 창으로 오인 안 함 |
| 실행 중 복구 창이 뜸 (`a1_smoke`) | 약 70초 뒤 FAIL · 분류 `모름` | **15.5초**에 멈춤 · 할 일을 말함 |
| 복구 창이 이미 떠 있음 | 같음 | 시작 전 검사 **0.2초** |
| 정상 잡 (`a1_smoke`) | 성공 | **1.7초 성공** (5초 검사가 성공 경로를 안 막는다) |

## BridgeTalk 이 약 70초에서 끊는 이유

못 찾았다. 68·69·69·74초로 네 번 다 70초 언저리였고 bridge.jsx 는 `bt.send(600)` 을 준다. 이번 수정도 그 값에 기대지 않는다.

---

확인한 것: 위 네 경우 실제 실행, 복구 창과 시작 화면의 자식 창 뼈대, pytest 전부 통과, run.ps1 BOM·파싱
안 본 것:  화면 배율이 다른 PC 에서의 뼈대(같다고 보지만 안 재 봤다) · 복구 창을 '안전 모드' 등 다른 버튼으로 넘긴 뒤의 상태 · BridgeTalk 70초의 원인
뒷정리: 시험으로 띄운 복구 창은 내가 '계속' 을 눌러 넘기고 AE·포토샵을 정상 종료했다(사람 대신 누른 것은 내 시험 뒷정리뿐이다). 일러스트레이터는 B 것이라 안 건드렸다.
