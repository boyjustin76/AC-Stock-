# B(썸네일) → 총괄 · 2026-09-17 오류·비효율 원본 기록

이정찬 지시: 오류·비효율 났던 것을 **내용과 로그 그대로** 모아 넘긴다.
정리(`issue` / `constraint_note` 로 넣을지, 묶을지, 번호)는 총괄 판단.

- 범위: B 세션 2026-09-17. 09-16 이전 것은 `brand/EXTENDSCRIPT-TRAPS.md` 에 있다 → 옮길지 판단은 `2026-09-17_B_EXTENDSCRIPT-TRAPS_이관판단.md`
- 저장소: 브랜치 `local/newch-style`, 기준 커밋 `3516487`
- 같은 내용을 총관리 세션에 메시지로도 보냈다 (1/2·2/2). 이 파일이 원본이다.

---

## A. 일러스트레이터 — 모달 창에 스크립트가 멈춤 (3건, 같은 뿌리)

### A1. CS6 호환 저장 → 열 때마다 "이전 버전 텍스트" 창

수정 전 코드 (`tools/illustrator/build_rollad.jsx` · `build_live.jsx`)

```javascript
so.compatibility = Compatibility.ILLUSTRATOR17;
```

증상: 저장본을 COM 으로 다시 열자 PowerShell 이 300초 타임아웃. 출력은 이 한 줄뿐.

```
열린 문서: 0
```

화면 캡처의 창 문구

```
이 파일에는 이전 버전의 Illustrator에서 만든 텍스트가 포함되어 있습니다. 텍스트 레이아웃을
업데이트하면서 일부 문자 위치가 변경되었을 수 있습니다.
• 취소를 선택하여 파일 열기를 정지합니다.
• 확인을 선택하여 업데이트된 텍스트가 있는 파일을 엽니다.
□ 다시 표시하지 않기(D)   [확인] [취소]
```

라이브화면 .ai(`차트명가 NEW 라이브화면구성.ai`)도 알림 켠 채 열어 같은 창 확인.

열거값 탐색 로그

```
Error 1320: Invalid enumeration valueLine: 1->  var o=[]; var n=["ILLUSTRATOR17","ILLUSTRATOR24",...
```

try/catch 로 다시 잰 결과

```
ILLUSTRATOR17=Compatibility.ILLUSTRATOR17 ILLUSTRATOR24=Compatibility.ILLUSTRATOR24 ILLUSTRATOR25=없음
ILLUSTRATOR26=없음 ILLUSTRATOR27=없음 ILLUSTRATOR28=없음 ILLUSTRATOR29=없음 ILLUSTRATOR30=없음
기본=Compatibility.ILLUSTRATOR24
```

- 해결: 둘 다 `Compatibility.ILLUSTRATOR24` (커밋 `6e4b3f9`)
- 검증: 알림 켠 채 열고 40초 뒤 화면 캡처 — 창 없이 열림. 라이브화면 미리보기 6장 재빌드 전후 픽셀 평균차 `0.0`
- 함께 본 것: `DONTDISPLAYALERTS` 로 열면 창이 안 떠서 문제가 가려진다

```
옻칠판_롤링광고.ai | 아트보드 11 · 레이어 11 · 텍스트 26 (Gungsuh) · 이전버전텍스트 0 · 래스터 25 · 복합패스 0
차트명가 NEW 라이브화면구성.ai | 아트보드 6 · 레이어 5 · 텍스트 42 (Gungsuh) · 이전버전텍스트 0 · 래스터 29 · 복합패스 0
```

(두 번째 파일은 이때 아직 CS6 저장본이었는데도 `legacyTextItems` 0)

### A2. A1 창이 떠 있는 동안 같은 파일에 saveAs → 저장 실패

재빌드가 600초 도구 타임아웃을 넘겨 백그라운드로 넘어감. 화면 캡처

```
그림을 저장할 수 없습니다. 파일이 읽기 전용이거나 다른 응용 프로그램에서 사용 중이거나, 필요한 권한이
없습니다. 문서를 다른 이름으로 저장하거나 다른 폴더 또는 디렉토리에 저장하십시오.
ID: -54                                                    [확인]
```

이어서

```
오브젝트 "다른 이름으로 저장"은(는) 현재 사용할 수 없습니다.   [계속(C)] [멈춤(S)]
```

- 원인: 앞선 확인용 열기(A1)의 창이 뒤에 남아 파일을 잡고 있었다
- 해결: 창 닫기 → 남은 미저장 문서 `Close(2)` → 재빌드

### A3. 트팩 원본 .ai 의 끊긴 링크 창 → build_live 가 기다림

이정찬이 화면에서 발견·캡처

```
연결된 파일 "09012023_15.jpg" 를 찾을 수 없습니다. 다른 파일을 선택하려면 [바꾸기]를, 연결을 변경하지
않고 그대로 두려면 [무시]를 선택하십시오.
□ 모두에 적용(A)   [바꾸기(R)] [무시(I)] [취소]
```

원본(`260114_라이브화면구성(2026v).ai`)은 저장 금지라 링크를 안 고쳤다. 해결 코드 `build_live.jsx` `openRef()`

```javascript
var ui = app.userInteractionLevel;
app.userInteractionLevel = UserInteractionLevel.DONTDISPLAYALERTS;
try { refDoc = app.open(f); }
finally { app.userInteractionLevel = ui; }
```

- 커밋 `3516487`. **다음 빌드로 아직 재검증 안 함.**

---

## B. 창 닫기 자동화가 안 먹힘

### B1. SendKeys

```powershell
$w = New-Object -ComObject WScript.Shell
$null = $w.AppActivate("Adobe Illustrator"); Start-Sleep -Milliseconds 500; $w.SendKeys("%a"); Start-Sleep -Milliseconds 400; $w.SendKeys("%i")
```

→ 창 그대로 (캡처 확인). 작업표시줄 IME 표시 "가"(한글), 포커스가 Claude 앱.
ESC(`{ESC}`)는 한 번 먹힌 듯했으나 실제로는 A1 창이 뒤에 남아 A2 로 이어졌다.

### B2. UI Automation

```
버튼 못 찾음 — 창 목록:
260114_라이브화면구성(2026v).ai* @ 16.87%(RGB/미리 보기)  / illustrator
```

모달이 트리에 안 잡힌다.

### B3. 해결 — 좌표 클릭

```powershell
[M]::Click(787,552); Start-Sleep -Milliseconds 500; [M]::Click(1061,552)   # SetCursorPos + mouse_event
```

체크박스 "모두에 적용" → [무시]. 화면 1920x1080 을 가정한 좌표.

---

## C. 셸·스크립트

### C1. PowerShell 실행 정책 + 성공 여부를 안 본 mv

명령: `powershell -NoProfile -File run.ps1 build_live`

```
    + CategoryInfo          : ���� ����: (:) [], ParentContainsErrorRecordException
    + FullyQualifiedErrorId : UnauthorizedAccess
```

2차 피해 — 같은 bash 루프가 빌드 결과를 안 보고 실행

```bash
mv "$O"/미리보기_*.png "$O/강조색비교/$V/"
```

→ 본판 미리보기 6장이 비교 폴더로 옮겨짐. 두 번째 반복에서

```
mv: cannot stat '.../라이브화면/미리보기_*.png': No such file or directory
```

- 복구: 미리 떠둔 백업에서 되돌림, `cmp` 로 6장 일치 확인
- 해결: `-ExecutionPolicy Bypass` + 결과에 `OK 아트보드 6` 이 있을 때만 mv

### C2. bash while-read 가 마지막 줄을 빠뜨림 → jpg 1장 누락

```bash
while IFS=$'\t' read -r i n; do
  ...; cp "$S/jpg_out/ab$i.jpg" "$R/$sub/$n.jpg" && echo "덮어씀 $sub/$n.jpg"
done < <(tr -d '\r' < "$S/jpg_out/map.txt")
```

`map.txt` 를 jsx 가 `out.join("\n")` 으로 써서 끝 줄바꿈이 없었다. 출력은 "덮어씀" 7줄, ls 에서 하나만 옛 시각

```
-rw-r--r-- 1 user 197121 392186 17:56 .../원본(하이라이트 버전)_short/하이라이트_B_2.jpg
```

- 해결: 수동 복사 + 8장 `cmp` 일치 확인. 이후 jsx 는 `join("\n") + "\n"`

### C3. 인코딩 cp949

```
UnicodeEncodeError: 'cp949' codec can't encode character '—' in position 40: illegal multibyte sequence
```

(플러그인 마켓 목록 출력, worklog.db 조회 출력에서 두 번)

PowerShell `Start-Job` 결과와 run.ps1 결과줄

```
?묒뾽 ?곹깭: Running
  ���: OK ��Ʈ���� 6
```

- 해결: `PYTHONIOENCODING=utf-8` / `PYTHONUTF8=1`. PowerShell 쪽은 `grep "OK"` 로만 판정

### C4. COM 확인 명령이 도구 타임아웃(120s·300s)으로 백그라운드 전환

원인은 전부 A1/A3 창. **COM 이 답을 안 주면 화면부터 찍는 것**이 가장 빨랐다.

---

## D. PIL → Illustrator 재현 오차 (롤링광고 .ai)

### D1. 밑줄 1px 어긋남

`tools/style/roll_ad.py` `rule()` 이 소수 좌표로 그린다 — 기록된 값

```json
{"kind": "rect", "x0": 1160.0, "y0": 151.64000000000001, "x1": 2174.0, "y1": 158.64000000000001, "fill": "#EF2767"}
```

PIL 은 151~158행을 칠하고, AI 사각형은 소수 그대로라 위아래 행이 반만 칠해졌다. 비교 로그

```
수정 전  하이라이트_B_2  평균차 0.28 · 큰차 0.317%  (글자 14531 · 무늬 0 · 그 밖 4468)
수정 후  하이라이트_B_2  평균차 0.23 · 큰차 0.249%  (글자 14531 · 무늬 0 · 그 밖 408)
```

- 해결: `build_rollad.jsx` `addRect` 에서 `Math.floor`

### D2. 남는 차이 (허용)

- ▼ polygon: PIL 계단식 vs AI 안티앨리어싱, 판마다 340~408px
- 글자 획 가장자리 1px. 전체 큰차 0.25~0.53% (고정댓글 377x71 은 1.68%)

### D3. 좌표 규칙 (D 가 알려줌)

- PIL `rectangle` 은 끝 좌표 포함 → 폭 = x1 - x0 + 1
- `outline` 은 상자 안쪽으로 자란다 → AI 선 가운데 정렬이면 w/2 들여 그린다
- `bold=1` = `MaxFilter(3)` 사방 1px → 같은 색 선 2pt

---

## E. 비효율

### E1. build_live.jsx 752초

```
exit 0 · 752초
```

매번 24MB 트팩 원본을 열어 캡쳐를 문서 사이로 복사한다. 그중 몇 분은 A3 창 대기.
진행 중 "응답 없음" 표시 — 멈춤 여부를 CPU 로 판정

```
CPU 20초간 +4.1s · 응답 False · 3096MB
CPU 20초간 +4.1s · 응답 True · 3129MB
```

(⑰ 진짜 멈춤은 +1s·메모리 고정이었다 → 이번엔 작업 중으로 판정, 그대로 끝남)

### E2. build_rollad 재빌드가 A2 로 10분 넘게 막힘

### E3. 확인용 열기의 딜레마

알림을 끄면 사람이 겪을 창을 못 보고(A1 의 `legacyTextItems 0`), 켜면 창에 막힌다.
지금은 "알림 켠 채 Start-Job 으로 열고 40초 뒤 화면 캡처" 로 확인했다.

---

## F. 환경·설정

### F1. GitHub MCP 연결 실패

```
plugin:github:github (400): "Error POSTing to endpoint: bad request: Authorization header is badly formatted"
```

`.mcp.json`

```json
"Authorization": "Bearer ${GITHUB_PERSONAL_ACCESS_TOKEN}"
```

1차 — 안내한 명령을 자리표시자 그대로 실행

```
! powershell -c "[Environment]::SetEnvironmentVariable('GITHUB_PERSONAL_ACCESS_TOKEN','토큰','User')"
```

```
사용자 변수: 있음, 길이 2, 앞 11자 토큰
현재 프로세스: 없음
```

2차 — 값을 넣고 Claude Code 를 재시작해도 같은 오류. 터미널 창이 넣기 전부터 열려 있어 Claude 가 옛 환경을 물려받았다.
터미널을 새로 열어 해결 → `get_me` → `boyjustin76`.

자동 모드 차단 로그

```
Permission for this action was denied by the Claude Code auto mode classifier. Reason: [Credential Leakage].        (.mcp.json cat)
Permission for this action was denied by the Claude Code auto mode classifier. Reason: [Unauthorized Persistence].  (환경변수 설정)
Permission for this action was denied by the Claude Code auto mode classifier. Reason: [Credential Materialization]. (환경변수 조회)
```

⚠ 토큰 원문이 대화 기록에 남았다 — 재발급을 권고해 둠.

### F2. 플러그인 스킬 중복

`document-skills` 의 docx/pdf/pptx/xlsx 와 `skill-creator` 가 claude.ai 쪽 `anthropic-skills:` 와 두 벌 → D 가 두 플러그인을 껐다.

---

## G. 판단 착오

### G1. "저장소는 private" 전제가 틀림

인수인계 요약에 private 로 적혀 있었다. GitHub API 결과

```json
{"name":"AC-Stock-","full_name":"boyjustin76/AC-Stock-", ... ,"private":false, ... ,"default_branch":"claude/futures-youtube-video-edit-fhio4s"}
```

사용자 결정으로 공개 유지.

### G2. 롤링광고 폴더 이동 → 프리미어 미디어 10개 끊김

이정찬이 `롤링광고\옻칠판\` 내용을 위로 꺼내면서 `라이브_롤링 광고_1.prproj` 가 옛 경로를 물고 있었다.
D 가 prlinks 로 발견·복구 (`\옻칠판\` → `\` 45곳 치환, 끊김 0). **원본 로그는 D 가 가지고 있다.**
