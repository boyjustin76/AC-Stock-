# B(썸네일) → 총괄 · EXTENDSCRIPT-TRAPS.md 를 worklog.db 로 옮길지

이정찬 지시: `brand/EXTENDSCRIPT-TRAPS.md` 의 옛 항목을 `worklog.db`(`issue` / `constraint_note`)로 옮길지는 **총괄이 정한다.**

## 읽을 것

- **원문: `brand/EXTENDSCRIPT-TRAPS.md`** (커밋 `3516487` 기준, 315줄)
  - 같은 저장소에 있어서 사본을 여기 붙이지 않았다. 문서 스스로 "사본은 조용히 어긋난다(㉒)"고 적고 있어서다.
  - 총관리 세션에는 메시지(2/2)로 원문 전체를 이미 보냈다.
- `repo_file` 표에 이미 등록돼 있다: `"포토샵·일러스트레이터·AE·프리미어가 같은 ExtendScript 를 쓰면서 서로 밟은 함정 모음. 증상 → 원인 → 처방. 새로 밟으면 여기 적는다"`

## 판단에 필요한 사실

**항목 구성** (번호 · 누가 실측했나)

| 절 | 항목 | 실측 |
|---|---|---|
| 언어 자체 | ① 중첩 삼항 · ② `$.evalFile` · ③ JSON 없음 · ④ 문자열 안 진짜 줄바꿈 | B |
| 실행 환경 | ⑤ `$.getenv` 는 켜진 시점 · ⑥ 남이 쓰는 앱 | B |
| | ⑦ 프리미어가 켜져 있으면 BridgeTalk 가 프리미어의 AE 로 | D |
| 문서 다루기 | ⑧ 닫은 문서 읽기 · ⑨ 문서 쌓임 · ⑨-2 CS6 저장 창 · ⑨-3 끊긴 링크 창 | B |
| | ⑩ mogrt 내보내기가 dirty 프로젝트 저장 · ⑪ 프리컴프 필수 속성 `canAdd=false` | D |
| 좌표·측정 | ⑫ 텍스트 top/left · ⑬ bounds 갱신 지연 · ⑭ 캔버스 ±8172pt · ⑮ PNG `matte` · ⑰ 문서 간 래스터 `.width` 멈춤 | B |
| | ⑯ 쓰기 직후 확인은 거짓 실패 | AE 쪽 정정 (`b7ae887`) |
| | ⑱ 포토샵 `impliedFontSize` · ⑲ 초록 채널 키 `grain` | B (포토샵) |
| 원본 자산 | ⑳ png 줄여 자르지 말 것 · ㉑ 래스터 벡터화 · ㉒ 문서보다 코드가 최신 | B |
| 원칙 | 경로 박지 마라 · 값 한 곳 · 실측 확인 · 치환 재확인 · 색/모양 판별 · 실패 자리 직접 확인 · 한 번 재고 정하지 마라 · 남의 실측 요약 금지 | B·D |

**형태가 DB 와 맞는 정도**

- 대부분 **증상 → 원인 → 처방** 으로 적혀 있어 `issue`(symptom / root_cause / fix / verification / status) 칸에 들어간다.
- ⑤·⑦·⑪·⑭·⑲ 같은 것은 사고라기보다 **도구의 한계**라 `constraint_note`(topic / limit_value / workaround) 쪽에 가깝다.
- 원칙 절은 어느 표에도 딱 맞지 않는다 (`decision` 이나 `runbook` 후보).
- 코드 조각·긴 사연(⑯ "다섯 달 틀린 규칙", ⑰ "진단이 한 번 틀렸던 기록")은 한 칸에 넣으면 줄어든다.

**문서 상태**

- 번호가 정리돼 있지 않다: ⑫ 가 두 번 쓰였고, ⑭ 다음이 ⑮·⑯·⑰ 로 건너뛴다. ⑨-2·⑨-3 은 오늘 끼워 넣은 것.
- ⑨-2·⑨-3 은 `2026-09-17_B_오류·비효율.md` 의 A1·A3 와 같은 건이다 (중복 주의).
- 코드 주석이 이 문서를 번호로 가리킨다 — 예: `tools/illustrator/run.ps1` "(EXTENDSCRIPT-TRAPS ⑥)", `build_rollad.jsx` "(matte 끔: EXTENDSCRIPT-TRAPS ⑮)". 번호를 바꾸면 주석도 같이 바꿔야 한다.
- 문서 자체 원칙: "**남이 실측한 것을 요약해서 옮겨 적지 마라** — ⑦·⑩·⑪ 이 전해 들은 세 줄로 시작해 대상도 처방도 틀렸다." DB 로 옮긴다면 원문 위치를 남기는 방식을 고려해 달라.
