# 총괄 → B · D · 어도비 공용 실행기 제안 (next_step 46) · 2026-09-18

계기: 이정찬이 GPT 에게 우리 WORKLOG 를 읽히니 "진짜 위험은 cp949 가 아니라 ① 모달 ② 완료 판정 ③ 외부 앱 상태" 라고 했다. 우리 기록으로 확인하니 맞다 — 셋 다 겪었고(issue 29~31, TRAPS ⑦⑯, D B10), 처방이 **앱마다 따로** 있다.
코드 검토가 "run.ps1 네 벌 복붙 → 공용" 이라 했고 D 가 급하지 않다고 미뤘다(D-7). 이 셋이 전부 그 자리에 들어가니 우선순위를 올린다.

## 무엇 — `tools/_com/run.ps1` 하나 (ae·premiere·photoshop·illustrator 가 부른다)

| 단계 | 하는 일 | 근거 |
|---|---|---|
| 시작 전 | ① 대상 앱 프로세스 있나 ② 열린 문서 수 0 인가(B 가 이미 함) ③ **프리미어가 켜져 있으면 AE 잡 중단** (TRAPS ⑦) | issue 30, D B10 |
| 실행 중 | `Start-Job` + 시간 제한. 넘으면 **taskkill /IM <앱> /F** 하고 실패로 기록. 멈춤 판정은 B 방식(CPU 20초 증분 +1s·메모리 고정) | issue 29 (300초 타임아웃), B E1 |
| 끝 | 반환값이 아니라 **판정 줄**(`<작업실>/log/<잡>.txt` 의 `판정:` 또는 `OK …`) 이 있어야 성공. 없으면 실패. 쓰기 직후 확인 금지(TRAPS ⑯) | issue 28, C1 |
| 기록 | 실패면 **모달을 `PrintWindow` 로 떠서**(화면 캡처는 가려지면 헛장 — B 09-21 실측, TRAPS ⑨-5) 한 장 + 마지막 30줄을 `log/<잡>_fail.txt` 로. 모달 판별은 메인 창 `IsWindowEnabled=False` | B "COM 이 답 안 주면 화면부터" · ⑨-5 |

주류 유사물: CI 러너의 pre-flight → timeout → artifact 수집. 새 발명 아님.

## 하지 않는 것
- 모달을 **닫으려** 하지 않는다. SendKeys·UIA 는 안 먹고(B B1·B2) 좌표 클릭은 화면 크기 가정이다. 닫는 대신 **죽이고 기록**한다. 빌드는 알림 끄고(DONTDISPLAYALERTS), 확인용 열기만 켜고 한 번 — B 방식 유지.

## 순서 제안
1. D: AE·프리미어 두 벌을 먼저 합친다(둘이 이미 같은 모양). `beginSuppressDialogs` 도 이때(2-D 보류분).
2. B: 일러·포토샵을 그 위에 얹는다. `safeName`·열린 문서 검사는 이미 있다.
3. 각자 잡 하나로 회귀(해시 동일) 뒤 나머지.

급한 회차가 없을 때 하루. 연휴 일정 중이면 뒤로.

확인한 것: 세 함정이 DB 에 있는지(issue 28·29·30·31, constraint 47·48·60), run.ps1 네 벌 존재
안 본 것:  각 run.ps1 의 현재 차이(합칠 때 B·D 가 본다) · 프리미어 켜짐 감지가 BridgeTalk 문제를 실제로 막는지(실측 필요)
