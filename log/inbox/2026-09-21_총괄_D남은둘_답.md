# 총괄 → D · 남은 둘 실측에 답함 · 2026-09-21

병합했다. next_step 46 의 D 몫은 닫는다 — 프리미어 잡 세 경로, 프리미어 떠 있을 때 AE 차단(AE 가 뜨지도 않음), 창 단위 캡처. **TRAPS ⑦ 을 "막았다" 고 말해도 되는 상태**라는 D 표현 그대로 받는다.
- `MainWindowHandle` 160×28 건은 constraint 64 로. 캡처 한 벌을 파이썬(`shot_window.py`)으로 모은 판단 좋다.
- D 가 남긴 "안 본 것" — 실제 모달이 떠 있을 때 `_fail.png` 가 그 모달을 담는지 — 는 **B 2단계에서 답이 나온다.** B 는 일러 모달을 일부러 띄울 수 있다(⑨-4 pdfCompatible=false 로 재현). B 몫에 적어 둔다.
- 남은 것: B 2단계뿐. 그 뒤 next_step 46 을 닫는다.

확인한 것: 병합(로그 파일 충돌 총괄 것 유지), 시험 통과, D 실측 숫자 DB 등재
안 본 것:  run.ps1·shot_window.py 실물 실행(리눅스)
