# 총괄 → D · B · Jev 시험 설계 (E 것과 같은 형식) · 2026-09-21

이정찬: "D·B 도 Jev 를 활용해 테스트하면 좋을 것, 또는 알아서 테스트해 봐라 — E 처럼." Jev 가 뭔지·값·약점은 `2026-09-21_총괄_Jev_판정관_시험제안.md` 를 먼저 읽는다(글을 안 만들고 판단만, Choice/Score/Noul, 입력 $0.042/M·출력 무료, **셈·날짜·긴 state·부정문에 약함, 텍스트만**).
공통 규칙: **있는 자료로만 · 반나절 · 정답이 있는 것만 잰다 · 미공개 대본/이미지는 안 보낸다(텍스트만이라 이미지는 애초에 못 보냄)** · 결과는 숫자 + `확인한 것/안 본 것`. 키는 이정찬이 `.secrets/ac_keys.env` 에 `TYPESAFE_API_KEY` 로 넣는다(E 와 같은 키).

## D — 셋 중 둘 이상

| # | 자리 | 질문 형식 | 정답 자료(이미 있음) | 합격선 |
|---|---|---|---|---|
| D-1 | **잡 로그 성공/실패 판정** — 공용 실행기의 '판정 줄 없음 = 경고 통과' 를 근거 있는 판정으로 | Noul "이 로그는 잡이 정상으로 끝났음을 보여 준다" + confidence | 작업실 `log/<잡>.txt` 수십 개. D 가 성공/실패를 손으로 20개 이상 표시 | 20개 중 **18 이상** 일치, 틀린 것의 confidence 가 맞은 것보다 낮으면 채택 → run.ps1 의 경고 통과 자리에 Noul 을 붙이고 confidence < 0.7 은 그대로 경고 |
| D-2 | **대본 비트 분류** — scenes.py 규칙 2·3 (원리 비트 = 민차트 / 지표 설명 비트 = 그 지표만) | Choice {원리, 지표 설명, 그 밖} · Choice {찾을 사건: 고점 뒤 하락, 돌파, 되돌림, 교차, 횡보, …} | `log/data/ref_cha10/` — 대본 64줄 + 팀장이 실제로 고른 콘티 12장 | 12장 중 **10 이상** 팀장과 같음. 지금 규칙 기반 scenes.py 와 나란히 비교해 어느 쪽이 팀장에 가까운지 적는다 |
| D-3 | **오류 분류** — D 인박스의 A1~A9·B1~B11·C1~C12 를 우리 constraint_note 63개 중 어느 것인지 | Choice(제목 63줄이 선택지) | D 자신이 적은 것이니 정답을 D 가 안다 | 30건 중 **25 이상**. radar 3순위와 같은 시험 — 되면 radar 에 붙인다 |

**하지 말 것**: 캡처 이미지 판정(텍스트만 받는다) · 봉 격자 좌표 보정(셈) · 날짜 구간 고르기(날짜 못 읽음). 이건 지금 코드가 맞다.

## B — 셋 중 둘 이상

| # | 자리 | 질문 형식 | 정답 자료(이미 있음) | 합격선 |
|---|---|---|---|---|
| B-1 | **모달 문구 → 처리 종류** — 공용 실행기 2단계에서 PrintWindow 로 뜬 문구를 어떻게 다룰지 | Choice {이전버전 텍스트(무시하고 열기), 끊긴 링크(무시), 저장 실패(중단), PDF 오류(중단), 새 시퀀스(중단), 모름} | B 가 09-17~21 에 실제로 만난 모달 문구 5~6개 + TRAPS ⑨-2·⑨-3·⑨-4 | 전부 맞고 '모름' 에 confidence 가 낮게 나오면 채택. 틀린 게 하나라도 있으면 그 문구를 적어 온다 |
| B-2 | **썸네일 강조 대상 고르기** — 규칙 22 "부정당하는 대상을 빨강으로" | Choice(윗줄 낱말 중 하나) "이 문장에서 부정당하는 대상은?" | 완성본 10회차 중 빨강이 든 것(#7·#8·#10 등, `thumbnail_rule` 22~24 · `log/data/thumbnail_fx.json`) + 차12 B2·C2 | 있는 사례 전부 일치. 빨강 없는 회차에서 '없음' 을 고르는지도 |
| B-3 | **오류 분류** — B 인박스 A~G 20건을 constraint_note 63개 중 하나로 | Choice | B 자신이 안다 | 20건 중 **16 이상** |

**하지 말 것**: 픽셀 평균차·잉크 비율 같은 수치 판정(셈) · 색 코드 근접 판정(문서에 "RGB·hex 가까운지 못 잰다" 명시) · 아트보드 캡처 보고 판정(이미지 불가).

## 알아서 하나 더 (선택)
각자 자기 자리에서 **"판단인데 지금 코드 규칙으로 흉내 내고 있는 것"** 을 하나 골라 같은 형식(정답 자료·합격선·숫자)으로 잰다. 형식만 지키면 무엇이든 좋다. 형식 없는 "해 보니 좋더라" 는 안 받는다.

## 보고
`log/inbox/2026-09-2X_<세션>_Jev시험.md` — 표 그대로 숫자, confidence 분포 한 줄, 틀린 사례 원문, `확인한 것/안 본 것`. 셋 다 오면 총괄이 external_tool 14 를 adopt/rejected 로 정하고 어디에 붙일지 next_step 으로 쓴다.

확인한 것: 세 시험 다 정답 자료가 저장소나 로컬에 이미 있음(ref_cha10 · thumbnail_fx · 인박스 원문 · 작업실 로그)
안 본 것:  작업실 잡 로그가 실제로 몇 개 남아 있는지(D-1 표본) · 한국어 실측(E 시험 ①이 먼저 답한다 — E 가 한국어에서 막히면 D·B 는 시작하지 않는다)
