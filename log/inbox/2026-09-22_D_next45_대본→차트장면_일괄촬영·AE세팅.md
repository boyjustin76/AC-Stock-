# D → 총괄 · next_step 45 ①② — 대본 한 편 → 차트 장면 12장 → 콘티 한 장 → AE 프로젝트 · 2026-09-22

이정찬 목표(09-18): "스크립트를 주면 팀장 기준으로 참고할 차트 장면을 쫘자작 찍어 주고, 콘티와 AE 컴포지션으로 딱 세팅".
차10 대본으로 끝까지 돌렸다. 명령 셋이다.

```bash
python tools/mt5/batch_capture.py <대본> <폴더>        # 비트마다 골라서 찍기 (12비트 124초)
python tools/mt5/conti_sheet.py <폴더> --title 차10     # 콘티.png + AE 입력
.\tools\ae\run.ps1 d1_conti_build                       # AE 프로젝트 (20초)
```

결과물 (저장소 밖): `06_실험실/차10_차트장면/` (그림 12 · 콘티.png · 콘티.md · 콘티.json) ·
`02_AE작업실_aelab/conti/차10_차트장면.aep` (비트 컴포지션 12 + 전체 230.3초, 1920x1080 · 59.94fps, 가이드 레이어에 대본·고른 이유)

## 고르는 법

1. **움직임** — scenes.RULES(낱말) 먼저. 못 잡은 비트만 **Jev** 에게 "이 대목은 차트가 어떤 움직임을 보여 줘야 하나" (문 0.7).
   선택지 설명을 차트 말이 아니라 **'대본이 이런 말을 할 때 → 차트는 이렇게'** 로 바꾸니 3-4 가 0.46 → **0.97** 로 톱니를 골랐다.
   Jev 가 "특정 움직임 없음" 을 고른 1-1(도입 질문)·3-2(계약 수 원칙)는 기본값(추세)으로 둔다 — 맞는 답이다.
2. **종목·주기** — 팀장 그림 14장 중 9장이 BTCUSD M1 이었다(24시간 거래라 빈 구멍이 없다). 후보 BTCUSD M1·M5, US100. M1 에서
   같은 움직임을 찾아 점수 최고를 쓴다.
3. **구간** — 비트 사이 겹침 금지, 주말 구멍 있는 구간 제외.

## 팀장 그림과 견준 것 (차10, 둘 다 있는 비트)

| 비트 | 팀장 | 이번 | |
|---|---|---|---|
| 1-3 | 양쪽으로 쓸림 | 큰 꺾임 8번 | ○ |
| 2-1 | 박스 이탈 / 상승·하락·재상승 | 톱니 (Jev 0.83) | △ |
| 2-2 | 고점 뒤 급락 | 고점 뒤 하락 | ○ |
| 2-3 | 세 번 꺾이는 톱니 | 톱니 | ○ |
| 3-2 | 이어지는 하락 추세 | 추세 (방향은 안 봄) | ○ |
| 3-3 | 급락 뒤 좁은 박스 | 박스 | ○ |
| 3-4 | 위아래로만 흔드는 톱니 | 톱니 (Jev 0.97) | ○ |
| 4-1·4-2 | 200/20EMA + ADX | 같은 지표 · ADX 25 돌파 / 20EMA 이탈 | ○ |

## 오늘 잡은 구멍 (다섯)

1. **지표 목록이 쉼표에서 잘렸다.** MCP `chart_add_indicator` 는 인자 문자열을 쉼표로 나눈다 → `EMA200,EMA20,ADX` 는 EMA200 만, **오류 없이**.
   `+` 로 잇게 고치고(CMG_Shot 은 둘 다 받음), **찍기 직전 차트에 실제로 달린 지표를 세어** 모자라면 콘티에 경고. 예전 capture_scene 도 같은 구멍이었다.
2. **CMG_Shot 이 4.8초에 스스로 지표를 뗐다** — 밖에서 찍는 모드는 떼는 도중을 찍었다 → `KeepInds` (차트째 닫으니 사람 차트엔 안 남는다).
3. **창 제목** — HedgeHood 는 제목에 'MetaTrader' 가 없다 → terminal64 프로세스 창으로 찾는다.
4. **톱니 점수가 가격 그대로** — BTCUSD 가 늘 이기고 한 다리만 큰 톱니가 뽑혔다 → '가장 작은 다리 ÷ 구간폭'.
5. **지지선 이탈·쓸림에 전용 탐색이 없었다** — '고점 뒤 하락' 계산을 빌려 써서 점수가 100% 로 꽉 차 아무 구간이나 뽑혔다(1-2 는 떨어졌다 다시 오르는 그림).
   → 지지선: 앞 70% 에서 같은 바닥 2번 이상 + 뒤 30% 가 60% 이상 그 아래. 쓸림: 구간폭 30% 넘는 꺾임 5번 이상. 1-2 는 '지지 4번 확인 뒤 이탈' 로 바뀌었다.

## 넘길 것 / 남은 것

- **B 에게 넘길 자리**: 콘티·컴포지션에 회사 브랜드 요소(매수·매도 버튼, 틀, 로고 — .ai/.psd)를 얹는 일. 지금은 MT5 차트 + 메모뿐이라 아직 넘길 게 없다.
- **Jev 는 그림을 못 본다** — "찍힌 차트가 그 장면에 맞나" 는 D 가 본다. 오늘 12장 전부 눈으로 봤다.
- 비트 길이는 **추정**(글자 수 ÷ 6.82자/초, 숏폼 자막 실측). 롱폼 성우 속도는 안 쟀다. 타임코드가 오면 conti_in.json 의 sec 만 바꾼다.
- ③ 다른 회차로 검증 · ④ 회사 드라이브 올리기(이정찬) 는 아직.
- 방향(상승/하락)은 안 본다 — 3-2 '이어지는 하락' 에 상승 추세가 뽑힐 수 있다. 대본이 방향을 말하면 맞추는 것이 다음 일.

---

확인한 것: 차10 12비트 실제 촬영 3회(고치기 전·지표 고친 뒤·탐색 고친 뒤), 12장 전부 눈으로, 4-1·4-2 지표 셋이 차트에 실제로 달린 것(MCP 로 읽음), 사람 차트(US100 M2) 지표·자리 그대로, AE 프로젝트 저장·열림 화면, pytest 전부 통과
안 본 것:  다른 회차 대본 · AE 비트 컴포지션 하나하나 열어 보기(전체·폴더 구성만 봤다) · 방향 맞추기 · 롱폼 성우 속도
