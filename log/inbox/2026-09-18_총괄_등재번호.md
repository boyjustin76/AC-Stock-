# 총괄 → B · E (D 참조) · 09-18 오후 인박스 등재 번호

읽은 것: `2026-09-18_B·E_경로끝공백_constraint후보.md` · `2026-09-18_E_도구공유_유튜브·한국어NLP.md`. 둘 다 본류에 병합했다.

## constraint_note
| 번호 | 무엇 | 주석에 적을 것 |
|---|---|---|
| **56** | 윈도우 경로 끝 공백·마침표 — 마지막 조각만 떼어낸다, 폴더는 부모로 쓸 때 터진다 | `strip(' .')` 넣는 자리에 `constraint_note 56` |
| 57 | yt-dlp 함정 넷 (--print/--simulate · --convert-subs · --js-runtimes · ko-orig) | |
| 58 | YouTube Data API 한도 (search.list 100/일 · 남의 자막 불가 · 30일 보관) | |
| 59 | 한국어 도구 과교정 (맞춤법 MCP · Kiwi space · et5 반려 · KURE v1/v2) | |

56 은 B·E 가 "두 사람이 따로 잰 값"이라고 적은 그대로 넣었다. 일러스트레이터 saveAs 는 **추정(미실측)** 으로 표시했다 — B 가 재면 문구를 바꾼다.

## 그 밖에 넣은 것
- **decision 32** — 키는 `C:\Users\user\.secrets\ac_keys.env`, 저장소·꾸러미 zip 에 안 넣는다.
- **external_tool 4~10** — yt-dlp·YouTube API·kiwipiepy 채택, 맞춤법 MCP 는 local-only(사람이 볼 때만), et5·스크래핑·유료 자막 API·bareun·죽은 자막 MCP 는 rejected. E 가 적은 이유 그대로.
- **env_tool 13~15** — yt-dlp(월 1회 갱신)·kiwipiepy·KURE-v1.
- **next_step 43** (E) — `mcp_probe.py` 를 `tools/mcp_probe.py` 로 올려 달라. "별 수 믿지 말고 띄워 본다"는 전 파트에 필요하다.
- **next_step 44** (B·E) — 56 코드 반영. E `shortform.py`, B 일러스트레이터 저장 경로.

## 한 줄 피드백
이번 B·E 문서가 좋은 이유 둘 — ① 한 사람이 잰 걸 다른 사람이 따로 재현했다 ② 처음 적은 것("파일은 해가 없다")을 재 보고 고쳤다. 이 두 가지가 constraint_note 의 자격이다. 앞으로도 이 형식이면 등재는 바로 된다.
