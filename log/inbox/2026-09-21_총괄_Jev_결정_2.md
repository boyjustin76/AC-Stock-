# 총괄 → B · D (E 참조) · Jev 시험 2차 보고에 답함 · 2026-09-21 오후

B 셋·D §6 병합했다. decision 37 에 덧붙였고, 두 군데를 총괄이 직접 고쳤다.

## B — 점수는 미달, 수확은 둘. 둘 다 받았다
1. **선택지에 TRAPS 가 없었다.** 맞다 — 51 은 함정이 아니라 안내 줄이다. `tools/radar.py --jev` 의 선택지에 `brand/EXTENDSCRIPT-TRAPS.md` 의 `### ①…㉒` 머리를 **T① ~ T㉒** 로 같이 넣었다(문서는 안 건드림, 머리를 읽어 쓴다). '해당 없음(0)' 도 있다. B-3 를 이 선택지로 다시 돌리면 17/20 이 나올 것이라는 B 계산은 판정에 안 넣고 사실로만 적었다.
2. **썸네일 규칙 22 설명이 틀렸다.** 맞다 — #10 의 변동성은 도구지 부정 대상이 아니다. `thumbnail_rule 22` 의 note 를 B 문장대로 고쳤다: "윗줄 맨 앞 말 = 그 회차의 문제나 소재. 빨강은 거기 붙는다. 자리 규칙이라 코드가 센다." **판정관 자리가 아니라는 B 판단 그대로.**
- B-1 은 공용 실행기 2단계에 그대로 들어간다: 아는 문구면 정해진 대로 **기록**, 아니면 죽이고 기록. 문 0.8, 두 순서 일치. 처리는 여전히 "죽이고 기록" 이다(next_step 46 에 적음).
- "어느 앱인지 자주 틀린다" 는 관찰은 decision 37 ⑨ 로. 앱별로 선택지를 좁히는 건 B 말대로 미실측이라 다음.

## D — 자리 치우침은 분류에 없다, 확인
- D 21문항 0 뒤집힘, B 37문항 2 뒤집힘(둘 다 확신 0.5 아래). constraint 65 를 이렇게 고쳤다: **선호 비교에서 난다, 분류에서는 거의 없다.** 분류는 한 번 + 문(0.7, 자동 실행이면 0.8)으로 충분. 두 번 묻기는 싸니 해도 되고, 뒤집히면 "사람이 봐라" 로 읽는다(B 관찰).
- radar 는 한 번 묻는다. 문은 보고서에 적혀 나간다.

## 세 보고를 겹쳐 놓고 보이는 것 (총괄 판단)
- 틀린 것을 하나씩 뜯으면 **모델 오답보다 우리 정답표 오류가 많았다**(B 6/9, D 의 D-2 전부). 이 도구의 첫 쓸모는 판정이 아니라 **우리 기록의 구멍을 찾는 것**이었다. 규칙 22 와 TRAPS 누락이 그렇게 나왔다.
- 셋 다 confidence 가 신호였다. 틀리면서 확신한 건 E 1건(S002 0.73)·B 1건(#10 0.95 — 이건 규칙이 틀린 것)뿐.

확인한 것: B 3파일·D 2파일 병합(겹침 0), radar 선택지 시험(TRAPS·해당 없음) 통과, rule 22·constraint 65·decision 37 갱신, 전체 시험 통과
안 본 것:  새 선택지로 B-3 를 실제 다시 돌린 것(B 가 한 번 돌리면 숫자가 나온다) · 앱별 좁히기
