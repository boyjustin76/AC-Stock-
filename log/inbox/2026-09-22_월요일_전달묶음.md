# 이정찬 → B · D · E · 월요일(09-22) 전달 묶음 — 총괄이 09-18 저녁에 모아 둠

이정찬이 퇴근해 못 전한 것을 한 장으로. 각자 자기 줄만 보면 된다. 원문은 링크된 파일.

## 이정찬 (5분, 첫 번째로)
- **토큰·Gemini 키 재발급** → `C:\Users\user\.secrets\ac_keys.env` 에 넣는다. issue 35 (유일한 open) · next_step 36. 킴 지적: "보안 이슈는 대기 항목이 아니라 시한부다". 발급 두 개를 한 번에.
- U-7: 로컬 훅(`~/.claude/hooks/hookify_reason.py`)과 bypass 모드 되돌리기 — 막는 쪽이 저장소에 있다(D 실측).

## 모두
- **완료 보고 두 줄** `확인한 것 / 안 본 것` — `log/inbox/_완료보고_양식.md` (decision 33). 다음 회신부터.
- **저장소 guard 규칙 4 추가** — 역슬래시 든 heredoc 을 막는다. 막히면 Write/Edit 도구로 파일을 쓰고 명령은 그 파일을 부른다. 규칙 완화 요청은 안 받는다(decision 34).
- **save.py** — 총괄 clone 이 아니면 `--only` 또는 `AC_SAVE_SCOPE` 없이 안 돈다. 각자 셸에 `AC_SAVE_SCOPE` 를 두면 편하다.
- **경로 리터럴 시험** `tests/test_no_path_literals.py` — 코드에 `C:/…` 를 새로 박으면 pytest 가 실패한다. labdir·환경변수·config 로.

## D
- `2026-09-18_총괄_저녁병합.md` — 인수인계 문서의 "save.py 는 --no-push" 줄 삭제(옛말).
- `2026-09-18_총괄_공용실행기_제안.md` — next_step 46. AE·프리미어 run.ps1 합치기부터. 급한 회차 없을 때 하루.
- `00_먼저읽기.md` 의 옛 경로 한 줄(E 지적).

## B
- `tools/style/trad.py:73` 폰트 경로 `C:\Windows\Fonts\batang.ttc` 는 BASELINE(1)에 적어 뒀다 — D 파일. 환경변수로 빼면 지운다.
- 공용실행기 제안 2단계(일러·포토샵). 일러스트레이터 saveAs 끝 공백은 실측 뒤 constraint 56 문구 갱신.

## E
- **뜻의 거리 다음은 판단이다** (이정찬 09-18). KURE 는 거리를 재고 있다 — 맞다. 빠진 건 "비슷한 둘 중 팀장이 고를 것". 모델을 키워도 안 풀리는 축이다. 두 가지를 부탁한다.
  1. `banner_model.py` (또는 `grade_draft.py`) 에 **LLM 판정관 단계** — 팀장 기준 5가지 + 합격 쌍 8개를 보여 주고 최상위 모델이 후보를 채점. 띠(거리)로 거른 뒤 판정관이 순위. 지금 되는 방법.
  2. **팀장 선택 기록** — 회차마다 팀장이 고른 문구와 버린 문구를 `tools/theone/` 에 쌍으로 쌓는다. 수십 쌍이 되면 그때 선호 학습. 병목은 모델이 아니라 이 표본이다.
- `2026-09-18_총괄_등재번호.md` — constraint 56~59 번호를 주석에.
- `tools/theone/pairs.py:61·64` EXTRA_SRT 경로 리터럴 → 환경변수/설정으로. 고치면 `tests/test_no_path_literals.py` BASELINE 에서 지운다.
- `tools/cutedit/prlinks.py:2` docstring 의 `\`` 가 DeprecationWarning(invalid escape) — 앞에 `r` 하나.

## 총괄이 남긴 것 (확인용)
확인한 것: 위 항목 전부 본류에 들어 있음(커밋·시험 통과), 인박스 파일 존재
안 본 것:  로컬 셋이 guard 4 와 save.py 새 기본값을 실제로 어떻게 겪는지 — 첫 주 오탐 보고를 받는다
