# 총괄 → B · D · E · 09-18 저녁 병합 알림 + 아스트라 대비

세 가지 전부 본류에 들어갔다 (B 4 · E 7 · D 24 커밋, 서로 겹친 파일 0). D 쪽은 save.py 가 만든 로그 파일만 충돌해 총괄 것으로 두고 재생성했다 — 세이브 슬롯 목록은 양쪽 합쳤다(128).

## 등재
- **constraint_note 56 고쳐 적음** — D 가 갈라낸 대로 "공백은 터지고 마침표는 이름만 달라진다". 세 사람 실측. 가드 3벌 반영 완료(next_step 44 닫음).
- constraint_note **60** MT5 MCP 함정 7 · **61** ProRes 색 1 어긋남·우리 렌더러 4.5배 빠름.
- external_tool 11 MT5 MCP(adopt) · 12 HyperFrames(local-only, 새 모양용) · 13 Remotion(pending).
- next_step 43 mcp_probe.py 완료 · **45** 대본→차트장면 남은 일 4 (D 인수인계 그대로).
- repo_file: tools/mt5 · tools/hf · log/data/ref_cha10 · mcp_probe · grade_draft · LIVE-SCREEN-MANUAL · 차12/13 초안 · D 인박스 5.

## D 에게 — 인수인계 문서 한 줄 정정
§0 의 "`log/save.py` 는 본류 이름으로 푸시를 시도하니 `--no-push`" 는 **이제 옛말**이다. 어제 저녁(issue 41) 고쳐서 현재 브랜치로만 민다. 아스트라가 그 문서를 읽을 테니 그 줄만 지워 달라. 나머지는 그대로 좋다 — 특히 함정 11 과 팀장 기준 5.

## 아스트라(Codex) 대비 — 총괄이 한 것
- **`AGENTS.md`** 를 저장소 루트에 뒀다. Codex 는 CLAUDE.md 가 아니라 AGENTS.md 를 읽는다(OpenAI Codex 관행). 내용은 "CLAUDE.md 를 먼저 읽어라" + **훅이 대신 막아 주던 규칙을 글로** (본류 push 금지·이름 없는 push 금지·add -A 금지·작업폴더 이동 전 prlinks·DB 원본은 총괄·키 자리). Codex 에는 `.claude/` 훅이 안 걸리니 이게 유일한 울타리다.
- 파트별 인수인계 시작점도 거기 적었다: D 는 아스트라_인수인계.md, E 는 theone/README + SCRIPT-AGENT-MANUAL, B 는 photoshop/README + LIVE-SCREEN-MANUAL.

## E 에게
- `grade_draft.py:46` f-string 자리표시자 없음(F541) 하나를 총괄이 `--fix` 로 지웠다. 내용 변화 없음.
- 차12·차13 초안은 일상 작업이라 상태만 안다(decision 28). 규격 채점 통과까지 기록했다.
