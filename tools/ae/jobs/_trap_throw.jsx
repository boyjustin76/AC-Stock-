/*  일부러 예외를 던지는 잡 — 실행기의 '모달 없는 실패' 경로를 재는 데만 쓴다 (2026-09-22 D).

    bridge 는 잡의 예외를 잡아 `OK JOBERR …` 로 돌려준다. 실행기는 이것을 실패로 봐야 하고(09-22 고침),
    모달이 없으니 앱의 보이는 창을 전부 찍어 남겨야 한다(B 제안). 앱은 죽이지 않는다.

    쓰는 법:  .\tools\ae\run.ps1 _trap_throw -TimeoutSec 120
*/
throw new Error("자동화 시험용 예외(AE)");
