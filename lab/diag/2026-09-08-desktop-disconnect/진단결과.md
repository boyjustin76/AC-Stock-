# 진단 결과 — 사무실 PC 데스크탑 앱 "컴퓨터에 연결할 수 없습니다" (2026-09-08, 총괄)

번들: bundleId f60a6da3 · 앱 1.46388.4 · win32 x64 · 생성 2026-09-08 10:32 KST (17파일, 이 폴더).

## 결론 한 줄
**네트워크·절전 문제가 아니다.** 지금 켜져 있는 앱(디바이스 `desktop-pb0pvbq`, Windows 스토어 MSIX 설치본)의
로컬 세션 저장소가 **비어 있다**(세션 0개). 사이드바의 E·B·D 세션은 계정 서버 목록에서 내려온 것이고,
그 세션들의 "호스트 컴퓨터"는 **지금 앱과 다른 디바이스 정체성**(이전 설치본/이전 로그인 상태)에 묶여 있다.
그 호스트가 다시 살아나지 않으니 "절전 모드이거나 오프라인"으로 표시되는 것이다. 앱 재시작으로는 안 풀린다
(실제로 10:24·10:25 두 번 재시작했지만 동일).

## 근거 (로그 원문)
1. main-log 3회 기동 전부 동일:
   - 09:56:55 `Initialization succeeded … existingSessions=0`
   - 09:56:55 / 10:24:40 / 10:25:06 `Loaded 0 persisted sessions from …\AppData\Roaming\Claude\local-agent-mode-sessions\<account>\<org>`
2. history-import: `local-agent-mode-sessions … records=0 folders=0`, `claude-code-sessions … records=0 transcriptsPresent=0`,
   `Automatic copy marker: absent (device not yet claimed)`, `Identity: … recorded slots: none (no config file)`.
3. 설치 형태: `Install kind: msix (windowsStore=true)` + `[MSIX] Filesystem virtualization active — …\Packages\Claude_pzs8sxrjxfjjc\LocalCache\Roaming\Claude exists`.
   → 스토어 판은 데이터를 `LocalCache\Roaming\Claude` 로 가상화해 쓴다. 직접 내려받은 exe 판은 진짜 `AppData\Roaming\Claude` 를 쓴다.
   **두 설치본이 한 PC 에 공존하면 서로의 세션을 못 본다.**
4. 09-07 오후: 14:31~14:33 웹 번들 오류 `preload-helper … does not provide an export named 't'` 4회(배포 직후 캐시 불일치) →
   14:37 · 16:44 · 17:16 **로그아웃 3회**(cowork-vm-log). 로그아웃/재로그인은 디바이스 페어링을 새로 잡는 흔한 계기다.
5. 배제: reachability 전 항목 ✓(claude.ai 155ms, api 327ms; 실패는 `*.claudemcpcontent.com` DNS 하나 — MCP 콘텐츠 CDN, 무관),
   메모리 32GB 중 25GB 여유, MCP 서버 0개·사고 0건, 업데이트 대기 없음. CORS 메트릭 오류(10:25:38)는 잡음.

## 사무실 PC 에서 확인할 것 (PowerShell, 읽기 전용)
```powershell
# ① 세션 데이터가 어느 폴더에 남아 있나 — 둘 중 하나에 세션 폴더가 있으면 그게 원래 호스트
Get-ChildItem "$env:APPDATA\Claude\local-agent-mode-sessions" -Recurse -Depth 2 | Select FullName
Get-ChildItem "$env:LOCALAPPDATA\Packages\Claude_pzs8sxrjxfjjc\LocalCache\Roaming\Claude\local-agent-mode-sessions" -Recurse -Depth 2 | Select FullName
# ② Claude Code 대화 기록(로컬 세션의 실체) — 있으면 터미널에서 이어받을 수 있다
Get-ChildItem "$env:USERPROFILE\.claude\projects" | Select Name, LastWriteTime
# ③ 설치본이 두 개인가 (스토어 MSIX + 직접설치 exe)
Get-AppxPackage *Claude* | Select Name, Version, InstallLocation
Get-ItemProperty HKCU:\Software\Microsoft\Windows\CurrentVersion\Uninstall\*, HKLM:\Software\Microsoft\Windows\CurrentVersion\Uninstall\* -ErrorAction SilentlyContinue | ? DisplayName -like "*Claude*" | Select DisplayName, DisplayVersion, InstallLocation
```

## 처방 (①~③ 결과에 따라)
- **③에서 설치본이 둘이면**: 세션은 다른 쪽(직접설치 exe) 것이다. 그 앱을 실행하면 세션이 바로 "연결됨"이 된다.
  이후 한 쪽만 남긴다 — 스토어 판을 지우고 exe 판으로 통일 권장(가상화 경로 문제 재발 방지).
- **설치본은 하나인데 ②에 기록이 있으면**: 세션의 호스트 정체성만 잃은 것. 앱에서 그 세션을 "이어가기"는 못 하지만,
  저장소 폴더(`…\AC-Stock-`)에서 터미널 `claude --resume` 으로 같은 대화를 이어받아 새 세션으로 살릴 수 있다.
- **①②③ 전부 비어 있으면**: 로컬 기록이 지워진 상태. 세션을 새로 만들고 저장소로 인수인계한다 —
  E 는 `local/script-lab` 28c86d7, B 는 `local/thumb-ch11` 0652cac, D 는 `local/ae-lab` 426ec3f 이 마지막 커밋이라
  작업 손실은 없다. 각 세션 첫 메시지: "저장소 브랜치 X 의 마지막 커밋에서 이어라" + 해당 매뉴얼 경로.

## 재발 방지
- 앱은 **한 종류만** 설치·사용 (스토어 판과 exe 판 혼용 금지).
- 로컬 세션이 살아 있는 동안 **로그아웃하지 않는다** (웹 오류가 나면 새로고침/앱 재시작까지만).
- 각 로컬 세션은 작업 덩어리마다 커밋·푸시 — 이번처럼 호스트를 잃어도 저장소가 정본이 되게.
